# 웹 전용 화면 정보 밀도·PC 레이아웃 개선 계획

작성일: 2026-08-08<br>
상태: 1차 구현 완료 / 검증 완료 (2026-08-08)

## 1. 목표

모바일 브라우저를 고려해 좁게 만들어진 현재 화면을 웹 전용 서비스에 맞게 확장한다. PC에서는 양옆의 빈 공간을 업무에 필요한 요약·주의사항·빠른 동작·상세 정보에 사용하고, 모바일에서는 현재의 카드형 목록·하단 메뉴·스와이프 흐름을 유지한다.

이번 계획의 기본 방향은 다음과 같다.

- 이미 저장된 주문·입금·플랫폼·결제수단·구매계정·리뷰 정보를 우선 활용한다.
- 단순히 카드와 컬럼을 늘리기보다 사용자가 “무엇을 먼저 처리해야 하는지” 판단할 수 있는 정보를 추가한다.
- 데스크톱은 넓은 콘텐츠 셸과 `main + aside` 구조를 사용하고, 모바일은 `main` 아래로 보조 영역을 쌓는다.
- 기존의 주문 완료처리, 가상 스크롤, 자동추천 후보 계산, 크롬 익스텐션 기능은 변경하지 않는다.
- 1차 작업에서는 DB 스키마를 변경하지 않는다. 새 작업 이력·알림·사용량 집계가 필요해지면 별도 결정 후 진행한다.

## 2. 현재 확인한 구조와 문제 지점

현재 라우트와 주요 책임은 아래와 같다.

| 화면 | 현재 구조 | PC에서 개선할 점 |
| --- | --- | --- |
| `/` 구매장부 | `HomePage` + `OrdersTable`, `max-w-4xl`, 통계 3개와 미완료/완료 주문 목록 | 목록 옆의 운영 요약·처리 우선순위 영역이 없고, 넓은 화면에서 목록만 중앙에 남음 |
| `/dashboard` 대시보드 | `OrdersDashboard`, `max-w-screen-xl`, 기간 조회·KPI·월별/분류별 표가 세로로 이어짐 | 넓이는 확보되어 있지만 정보가 세로로 길고, 기간 판단과 다음 행동이 분리됨 |
| `/dashboard/monthly` 월별 상세 | `max-w-screen-md`, 월별 KPI 12개와 분류별 표 | PC에서 가장 큰 좌우 여백이 생기며, 숫자 요약과 실제 확인할 주문이 떨어져 있음 |
| `/menu-4` 자동 추천 | `CrawlOrdersPage`, 2페이지 슬라이더, 주문 추천/입금 추천을 한 번에 하나만 마운트 | PC에서도 모바일과 같은 폭·슬라이더 중심이라 목록 옆의 설명·요약을 활용하지 못함 |
| `/orders/new` 주문 추가 | `OrderDetailForm`, `max-w-4xl`, 여러 입력 카드가 한 줄 흐름 | 입력 중 현재 값의 요약, 누락 항목, 여러 계정 생성 결과를 한눈에 보기 어려움 |
| `/orders/detail` 주문 상세 | `OrderDetailForm`, 상단에 목록 이동 버튼만 있음 | 주문 상태·금액·배송·리뷰 진행 상태를 폼 옆에서 계속 확인하기 어려움 |
| `/settings` 설정 | `SettingsPanel` 내부 뷰 전환, 단일 세로 메뉴 | PC에서도 모바일식 드릴다운을 사용해 빈 공간이 생기고 설정 항목 간 관계가 약함 |
| 템플릿 추가/수정 | `PurchaseInfoTemplateForm`, `max-w-4xl`, 입력 폼 단일 열 | 입력값이 실제 카톡 붙여넣기 결과로 어떻게 보이는지 즉시 확인하기 어려움 |
| `/login` 로그인 | 중앙 카드형 로그인 | 인증 화면의 여백은 의도된 부분이지만 PC에서 서비스 설명과 로그인 목적을 함께 보여줄 수 있음 |

공통으로 `BottomMenu`는 현재 `max-w-4xl`이고, 인증을 제외한 모든 화면에 고정되어 있다. 모바일에서는 유지하되 데스크톱에서는 별도 내비게이션을 둘지 결정이 필요하다.

## 3. 공통 레이아웃 계획

### 3-1. 데스크톱 셸

기본안은 `1024px` 이상에서 다음 구조를 적용한다.

```text
┌────────────────────────────────────────────────────────────┐
│ 데스크톱 내비게이션 │ 페이지 헤더: 제목 · 상태 · 계정 · 주요 액션 │
├────────────────────┼───────────────────────────────────────────┤
│                    │ main 콘텐츠                               │
│                    │ ┌──────────────────────┬────────────────┐ │
│                    │ │ 주 업무 영역          │ 보조 정보/액션 │ │
│                    │ │ 목록·폼·표             │ 요약·주의·링크 │ │
│                    │ └──────────────────────┴────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

- 콘텐츠 컨테이너는 현재 `max-w-4xl`에 고정하지 않고, 화면 폭에 따라 `minmax(0, 1fr)`로 늘어나도록 한다.
- 기본 목표 폭은 콘텐츠 기준 `1280~1440px`로 두고, 너무 넓은 모니터에서는 읽기 어려워지지 않도록 내부 표·폼별 최대 폭을 따로 둔다.
- 메인/보조 영역은 12열 기준으로 나누되, 보조 영역이 짧은 화면에서 목록을 지나치게 좁히지 않도록 `8:4` 또는 `9:3` 비율을 사용한다.
- 페이지 헤더는 제목, 현재 범위/상태, 새로고침·추가·돌아가기 같은 해당 화면의 주요 액션을 한 줄에 배치한다.
- 보조 패널은 긴 화면에서 `sticky`를 사용할 수 있지만, 모바일의 고정 하단 메뉴와 겹치지 않도록 한다.
- 장식용 빈 카드나 의미 없는 그래프는 추가하지 않는다. 각 영역에는 “판단할 수 있는 정보” 또는 “바로 실행할 수 있는 동작”만 둔다.

### 3-2. 모바일 셸

- 현재 하단 메뉴, `safe-area-inset-bottom`, 모바일 카드/테이블 전환, 터치 가능한 최소 버튼 크기를 유지한다.
- 데스크톱의 보조 패널은 본문 아래로 이동하고, 중요한 경고·완료 액션은 목록보다 위에 둔다.
- 모바일에서는 검색·필터·부가 설명을 접을 수 있게 하되, 처리 건수와 핵심 상태는 접지 않는다.
- PC 전용 hover에 의존하는 삭제·상태 표시를 만들지 않는다. 현재처럼 모바일에서는 액션을 항상 보이거나 명확한 버튼으로 제공한다.
- 가로 스크롤이 필요한 표는 표 전체를 감싸고, 페이지 전체가 가로로 밀리지 않도록 한다.

### 3-3. 공통 상태·접근성

- 로딩·빈 상태·오류 상태도 넓어진 레이아웃에 맞는 영역을 사용하고, 오류에는 해당 화면으로 돌아가거나 다시 시도하는 액션을 둔다.
- 색상은 `DESIGN.md`의 따뜻한 캔버스, 흰색 패널, hairline 경계, primary blue를 따른다. 상태 색은 완료·주의·오류의 의미 전달에만 사용한다.
- 테이블 행·카드·아이콘 버튼의 키보드 포커스와 `aria-label`을 유지한다.
- 새 요약 수치에는 로딩 중 값과 데이터 없음(`-` 또는 `0`)을 명확히 구분한다.

## 4. 페이지별 정보 추가·배치 계획

### 4-1. `/` 구매장부

대상 파일: `src/components/pages/home-page.tsx`, `src/components/orders/orders-table.tsx`

#### 추가할 정보

- 상단 요약 스트립에 전체 주문·미완료·완료·완료율을 표시한다. 가능하면 미완료 구매원금도 함께 보여준다.
- PC 보조 패널에 `오늘 확인할 일`을 둔다.
  - 입금 미완료 건수와 미완료 구매금액
  - 미배송 건수
  - 예약 구매 시각이 가까운 주문
  - 완료됐지만 입금 정보가 비어 있거나 확인이 필요한 주문
- `빠른 작업`에 주문 추가, 자동추천, 대시보드 이동을 둔다.
- 현재 주문 행에 이미 있는 상품명·구매일·구매금액·플랫폼·결제수단·구매계정·배송/입금 상태를 우선순위가 보이도록 재배치한다.
- PC에서 필터를 항상 펼쳐두고 검색어·구매일·상태를 한 줄에 배치한다. 모바일은 현재처럼 필터 버튼을 눌러 여는 방식을 유지한다.
- 완료 주문을 펼쳤을 때 최근 완료 요약과 전체 대비 완료율을 함께 보여준다.

#### 배치

- 데스크톱: 상단 요약은 전체 폭, 그 아래 `주문 목록 8~9열 + 오늘 확인/빠른 작업 3~4열`로 배치한다.
- 주문 목록은 현재 가상 스크롤과 데스크톱 표/모바일 카드를 유지한다. 표의 남는 폭에는 `상태`, `입금 차액 또는 수익`, `예약/배송 정보` 중 선택된 항목만 추가한다.
- 모바일: 요약 카드 → 오늘 확인할 일 → 미완료 주문 → 완료 주문 순서로 쌓고, 주문 추가 FAB는 하단 메뉴와 겹치지 않게 유지한다.

#### 주의점

- 완료 주문은 현재 지연 조회되므로 완료 금액 기반 요약을 처음부터 보여주려면 가벼운 집계 조회가 필요하다. 이를 위해 완료 전체 목록을 무리하게 초기 로드하지 않는다.
- 기존 완료처리 입력, 금액 보정, 확인창, 삭제, 가상 스크롤은 동작을 그대로 둔다.

### 4-2. `/dashboard` 대시보드

대상 파일: `src/components/pages/dashboard-page.tsx`, `src/components/orders/orders-dashboard.tsx`, `src/lib/dashboard-stats.ts`

#### 추가할 정보

- 현재 선택 기간과 직전 동일 기간을 비교하는 구매금액·입금금액·수익·완료율 변화량을 표시한다.
- KPI에 누적 구매금액·누적 입금금액·미회수 원금·미완료 건수 외에 완료율 또는 미배송 건수를 추가한다.
- 월별 요약 표에서 월을 누르면 `/dashboard/monthly?month=...`로 이동하도록 연결한다.
- `운영 인사이트` 영역에 다음 중 기존 데이터로 계산 가능한 항목을 표시한다.
  - 가장 구매금액이 큰 플랫폼/구매계정
  - 미완료 금액이 큰 기간
  - 미배송 건수와 완료율
  - 선택 기간의 평균 수익
- 그래프는 **Recharts**를 사용한다. 차트가 로드되지 않거나 보조기술을 사용하는 경우에도 기존 표와 수치가 항상 남도록 한다.

#### 대시보드에 권장하는 차트 기능

우선순위를 나눠 구현하되, 아래 기능은 모두 기존 주문 데이터와 기간 집계로 만들 수 있는 범위다.

- **월별 구매·입금·수익 추이**: 구매금액/입금금액은 막대, 수익은 선으로 겹쳐 보여 세 금액의 흐름을 한 번에 비교한다.
- **완료율·배송률 추이**: 월별 완료율과 배송률을 0~100% 축의 선 또는 영역 차트로 보여 운영 상태를 확인한다.
- **미회수 원금 추이**: 월별 미완료 주문의 구매원금을 누적 막대로 표시해 자금이 묶이는 시점을 찾는다.
- **플랫폼·결제수단·구매계정별 비교**: 구매금액/입금금액/수익 중 하나를 선택해 가로 막대 순위를 바꿔 본다.
- **기간 비교 오버레이**: 현재 기간과 직전 동일 기간을 색상·증감률·툴팁으로 비교한다.
- **KPI 미니 스파크라인**: 누적 구매금액·입금금액·수익·미완료 원금 카드 안에 최근 월 흐름을 작게 표시한다.
- **차트 범례 토글**: 구매금액·입금금액·수익처럼 여러 지표가 겹치는 차트는 범례를 눌러 필요한 선만 남긴다.
- **차트 툴팁**: 월, 분류명, 건수, 금액, 비율을 원화·퍼센트 형식으로 표시하고 `tabular-nums`를 유지한다.
- **월별 상세 이동**: 월별 차트의 점/막대를 선택하면 해당 월 상세 화면으로 이동한다. 기존 `/dashboard/monthly?month=...` 경로를 재사용한다.
- **분류 차트와 표 동기화**: 분류 차트의 범례·순위와 아래 표의 정렬 순서를 일치시켜 차트와 숫자를 따로 해석하지 않게 한다.
- **미완료 집중 표시**: 미완료 금액이나 확인 필요 비중이 전체에서 높아지는 월은 주의 색으로 표시하되, 장식적인 색상 사용은 피한다.
- **반응형 차트**: PC에서는 충분한 높이와 범례를 사용하고, 모바일에서는 차트 높이를 줄이며 필요한 경우 내부 가로 스크롤을 사용한다.
- **빈 데이터·단일 데이터 처리**: 데이터가 없으면 빈 차트 대신 설명을 표시하고, 월이 한 개뿐이면 추세를 과장하지 않고 단일 값으로 보여준다.
- **표 기반 접근성 대체**: 차트 아래에 동일한 수치의 요약 표 또는 화면낭독기용 설명을 제공한다.

차트의 1차 핵심은 월별 금액 추이, 완료율/배송률, 분류별 가로 막대, 월별 상세 이동으로 한다. 스파크라인·범례 토글·기간 오버레이는 구현 난이도와 화면 밀도를 확인한 뒤 같은 작업에서 포함한다.

#### 배치

- 데스크톱: 기간 필터/대상 건수는 전체 폭, KPI는 4~5열, 다음 줄은 `월별 추이 8열 + 운영 인사이트 4열`, 하단은 플랫폼·결제수단·구매계정 표 3열로 배치한다.
- 월별 표는 가로 스크롤을 유지하되, PC에서는 충분한 폭을 사용해 금액·건수 열을 읽기 쉽게 한다.
- 모바일: 기간 필터 → KPI 2열 → 인사이트 → 월별 표 → 분류별 표 순으로 세로 배치한다.

### 4-3. `/dashboard/monthly` 월별 상세

대상 파일: `src/components/pages/monthly-dashboard-detail-page.tsx`

#### 추가할 정보

- 현재 월과 전월의 구매금액·입금금액·수익·완료율을 비교하는 작은 비교 영역을 둔다.
- 12개 KPI를 의미별로 `금액`, `운영 상태`, `수익성` 그룹으로 나눠 우선순위를 만든다.
- 월별 주문에서 바로 확인할 수 있도록 `미완료/미배송 주문` 요약과 최대 5건의 바로가기 목록을 추가한다.
- 플랫폼·결제수단·구매계정별 표에 건수와 비중을 표시한다. 비중이 필요하면 현재 월 주문 수에서 계산하고, 별도 집계 테이블은 만들지 않는다.

#### 배치

- `max-w-screen-md`를 제거하고 데스크톱에서는 `요약 8열 + 확인할 주문 4열` 구조를 사용한다.
- 월 이동 헤더는 전체 폭의 상단에 두고, 대시보드로 돌아가기와 현재 월 선택을 함께 제공한다.
- KPI는 PC에서 4열, 태블릿에서 3열, 모바일에서 2열로 배치한다.
- 하단의 플랫폼·결제수단·구매계정 표는 PC에서 3열, 모바일에서 세로로 쌓는다.

### 4-4. `/menu-4` 주문 내역 자동 추천

대상 파일: `src/components/pages/crawl-orders-page.tsx`, `src/components/pages/chrome-extension-install-guide.tsx`

#### 추가할 정보

- 현재 `대기 추천`, `자동 매칭`, `확인 필요` 수치를 유지하고, 자동 매칭률 또는 확인 필요 비율을 추가한다.
- 추천 대기 목록의 PC 컬럼에 상품명/원문 제목, 구매일·구매금액, 플랫폼, 결제수단, 구매계정, 처리 액션을 명확히 분리한다.
- 계정별 크롤링 상태·마지막 실행 결과·익스텐션 설치 안내를 보조 패널에 둔다. 현재 저장된 실행 상태와 메시지만 사용하고, 실행 이력은 새로 만들지 않는다.
- 목록에 플랫폼·계정·확인 필요 기준 필터를 추가한다. 모바일에서는 상단의 접이식 필터로 제공한다.

#### 배치

- 데스크톱 기본안: `추천 대기 목록 8~9열 + 크롤링 상태/익스텐션 안내/처리 가이드 3~4열`.
- 크롬 익스텐션 설치 안내는 PC에서는 보조 패널로 이동해 목록의 세로 길이를 줄이고, 모바일에서는 현재처럼 목록 위에 전체 폭으로 둔다.
- 두 번째 입금 자동추천 화면은 `입금 계좌 요약 3~4열 + 입금 미완료 목록 8~9열`로 배치한다.
- 입금 요약에는 미처리 건수, 총 입금액, 추천 후보가 없는 건수, 높은 일치 후보가 있는 건수를 표시한다.
- 입금 행을 펼쳤을 때 후보 카드는 현재의 미완료/완료 구분, 유사도 배지, 입금금액 일치, 완료처리/매핑완료 액션을 유지하고, PC에서는 행 아래 넓은 후보 영역을 사용한다.
- 모바일에서는 현재의 2페이지 스와이프와 한 열 목록을 유지한다. 현재 보고 있는 추천 페이지만 마운트하는 최적화도 유지한다.

#### 선택이 필요한 부분

- 현재 좌우 화살표·스와이프 슬라이더를 PC에서도 유지할지, PC에서는 상단 탭/세그먼트로 바꿀지는 아래 질문에서 결정한다.
- 양쪽 추천 화면을 동시에 마운트하면 PC 폭은 더 잘 쓸 수 있지만, 입금 추천 데이터와 주문 추천 데이터가 동시에 조회될 수 있으므로 기본안은 한 화면만 마운트하는 것이다.

### 4-5. `/orders/new` 주문 추가

대상 파일: `src/components/pages/new-order-page.tsx`, `src/components/orders/order-detail-form.tsx`

#### 추가할 정보

- 우측 요약 패널에 입력 중인 상품명·구매일·구매금액·선택 플랫폼·결제수단·구매계정·배송/입금 상태를 실시간으로 표시한다.
- 필수값 충족 여부와 저장 가능 여부를 체크리스트로 보여준다.
- 구매계정을 여러 개 선택했을 때 생성될 주문 수와 주문번호가 비워지는 규칙을 저장 전에 안내한다.
- 입금금액이 입력되면 구매금액 대비 차액/예상 수익을 보여주고, 미입력 상태는 “아직 계산하지 않음”으로 구분한다.
- 기존 카톡 붙여넣기 정보·구매정보·AI 리뷰·완료정보 카드의 내용을 PC에서 두 열로 나눠 긴 스크롤을 줄인다.

#### 배치

- 데스크톱: `입력 폼 8열 + 실시간 요약/저장 전 확인 4열`.
- 폼 안에서는 구매정보를 먼저 두고, 카톡 템플릿·AI 리뷰·완료정보를 보조 순서로 둔다.
- 저장 액션은 PC에서 우측 요약 패널 상단 또는 하단에 sticky로 두고, 모바일에서는 폼 마지막의 전체 폭 액션으로 둔다.
- 현재 `OrderDetailForm`의 저장·완료·확인창·템플릿·AI 리뷰 동작을 재구현하지 않고 기존 상태를 확장해 요약에 연결한다.

### 4-6. `/orders/detail` 주문 상세

대상 파일: `src/components/pages/order-detail-page.tsx`, `src/components/orders/order-detail-form.tsx`

#### 추가할 정보

- 헤더에 상품명, 주문 상태, 구매일, 플랫폼/구매계정, 목록으로 돌아가기 및 저장/완료 액션을 배치한다.
- 우측 요약 패널에 구매금액·입금금액·수익·입금 상태·배송 상태·리뷰 사진/글자 수를 표시한다.
- `created_at`·`updated_at`은 “최초 등록/최근 수정”으로만 보여주고, 실제로 존재하지 않는 상세 변경 이력처럼 표현하지 않는다.
- 미배송·미입금·리뷰 미작성 등 현재 주문 데이터로 판단할 수 있는 확인 항목을 우선순위 순으로 보여준다.

#### 배치

- 데스크톱: `주문 편집 폼 8열 + 상태/금액/다음 행동 4열`.
- 폼의 기존 섹션은 모바일에서 현재 순서를 유지하고, PC에서는 독립적인 카드끼리 2열로 배치한다.
- 완료·저장 액션은 데스크톱에서 우측 패널에 고정할 수 있고, 모바일에서는 하단 메뉴와 충돌하지 않는 일반 액션 영역으로 둔다.

### 4-7. `/settings` 설정 홈과 하위 뷰

대상 파일: `src/components/pages/settings-page.tsx`, `src/components/settings/settings-panel.tsx`

#### 추가할 정보

- 설정 홈에 닉네임/이메일 카드와 함께 템플릿 수, 플랫폼 수, 결제수단 수, 구매계정 수를 요약한다.
- 각 메뉴에 현재 등록 건수 또는 “설정 필요” 배지를 추가한다.
- 계정 화면에는 닉네임·이메일·로그아웃을 명확히 묶고, 개인정보를 더 수집하는 항목은 추가하지 않는다.
- 템플릿 화면에는 제목뿐 아니라 저장된 필드가 몇 개인지, 최근 수정 시각을 표시한다. 실제 내용 미리보기는 민감 정보 노출 여부를 고려해 선택한다.
- AI 설정 화면은 왼쪽에 입력 목적·개인정보 주의사항, 오른쪽에 기존 폼을 배치한다.
- 플랫폼·결제수단·구매계정 화면은 목록, 색상/숨김/삭제 상태, 새 항목 추가를 한 영역에서 확인할 수 있도록 한다. 사용 중인 주문 수를 표시하려면 추가 집계 조회가 필요하므로 결정 후 진행한다.

#### 배치

- 데스크톱: 글로벌 사이드바의 `설정` 항목을 펼쳐 하위 설정 메뉴를 보여주고, 설정 화면 안에 별도의 두 번째 사이드바는 만들지 않는다. 선택한 설정 화면은 글로벌 사이드바 오른쪽의 전체 콘텐츠 영역을 사용한다.
- 설정 홈에서는 계정 요약을 전체 폭으로, 나머지 설정 링크를 2~3열 카드 또는 그룹으로 배치한다.
- 모바일에서는 현재처럼 한 화면에서 메뉴를 선택하고 하위 화면에 들어가는 드릴다운을 유지한다.
- 글로벌 사이드바의 하위 메뉴와 현재 `SettingsPanel`의 내부 view 상태를 연결하고, `?view=account` 진입 방식은 우선 유지한다. URL 라우트 분리는 별도 요구가 있을 때만 한다.

### 4-8. `/settings/purchase-templates/new` 및 `/settings/purchase-templates/detail`

대상 파일: `src/components/pages/new-purchase-template-page.tsx`, `src/components/pages/purchase-template-detail-page.tsx`, `src/components/purchase-templates/purchase-info-template-form.tsx`

#### 추가할 정보

- 입력 필드를 `식별 정보`, `배송 정보`, `계정/정산 정보`로 묶어 사용자가 빠르게 찾게 한다.
- 우측에 입력값으로 생성되는 카톡 붙여넣기 결과 미리보기를 제공한다. 미리보기는 기존 `buildKakaoPasteLine` 공통 로직을 재사용한다.
- 제목 필수 여부, 입력된 필드 수, 복사 가능한 상태를 보여준다.
- 수정 화면에서는 최근 수정 시각과 “설정 목록으로 돌아가기”를 명확히 두며, 삭제나 복제는 별도 요구가 없으면 추가하지 않는다.

#### 배치

- 데스크톱: `템플릿 입력 7열 + 실시간 미리보기/도움말 5열`.
- 저장 버튼은 헤더와 폼 마지막 중 한 곳에서만 제공해 중복 액션을 피한다. 긴 폼에서는 우측 미리보기 상단에 sticky 저장 상태를 둘 수 있다.
- 모바일에서는 입력 폼을 먼저, 미리보기를 그 아래에 배치하고, 저장 버튼은 전체 폭으로 둔다.

### 4-9. `/login` 및 비로그인 상태

대상 파일: `src/components/pages/login-page.tsx`, `src/components/pages/home-page.tsx`, `src/components/auth/landing-auth-panel.tsx`, `src/components/auth/login-form.tsx`

- `/login`과 로그인 전 `/`는 같은 서비스 소개형 랜딩 구조를 공유한다. 데스크톱 기본안은 `서비스가 해결하는 일/간단한 안내 5열 + 로그인 카드 7열`의 split layout이다.
- 안내 영역에는 구매장부·자동추천·대시보드의 핵심 용도와 로그인 후 첫 행동만 간단히 보여주고, 마케팅 문구를 과도하게 늘리지 않는다.
- 모바일은 현재 중앙 로그인 카드와 안내 문구를 유지한다.
- 로그인 후 `/`는 기존 구매장부로 전환한다. 비로그인 상태와 로그인 상태의 화면 목적이 혼동되지 않도록 로그인 전용 랜딩 콘텐츠와 구매장부 콘텐츠를 동시에 보여주지 않는다.
- 랜딩 안내는 `LandingAuthPanel`·`LoginForm`을 재사용해 `/login`과 홈의 비로그인 상태에서 문구·간격·모바일 동작이 달라지지 않게 한다.

### 4-10. 공통 크롬 익스텐션 안내

대상 파일: `src/components/pages/chrome-extension-install-guide.tsx`

- 자동추천 PC 보조 패널에 들어갈 수 있도록 현재 카드의 폭·버튼·접기 동작을 재사용한다.
- 모바일에서는 다운로드 버튼이 전체 폭으로 보이고 설치 단계는 접힌 상태를 유지한다.
- 익스텐션 버전·ZIP·설치 내용은 현재처럼 보존한다. 별도의 스토어 배포나 자동 업데이트 기능은 이번 계획에 포함하지 않는다.

## 5. 데이터·구현 범위 원칙

- 새 페이지를 만들기보다 기존 `HomePage`, `OrdersTable`, `OrdersDashboard`, `CrawlOrdersPage`, `OrderDetailForm`, `SettingsPanel`, `PurchaseInfoTemplateForm`을 확장한다.
- 공통 수치 계산은 `src/lib/dashboard-stats.ts`에 추가하고, 페이지별로 같은 계산을 복제하지 않는다.
- 템플릿 미리보기는 `src/lib/kakao-purchase-paste.ts`, 색상은 `src/lib/color.ts`, DB 조회는 기존 Supabase client/쿼리 구조를 재사용한다.
- Recharts를 도입하면 `package.json`과 `package-lock.json`에만 의존성을 추가하고, 차트 표시를 담당하는 클라이언트 컴포넌트와 수치 계산을 담당하는 `dashboard-stats.ts`를 분리한다. 차트 라이브러리가 주문 데이터 조회나 업무 상태 변경을 직접 담당하지 않게 한다.
- 완료 주문 전체 목록을 초기 로드하거나 자동추천 두 페이지의 대량 데이터를 동시에 조회해 화면을 넓히는 방식은 피한다.
- 새 요약에 필요한 값이 현재 화면에 이미 로드된 데이터로 계산되지 않으면 다음 순서로 판단한다.
  1. 현재 쿼리 결과로 계산할 수 있는지 확인한다.
  2. 가능하면 count/sum 같은 가벼운 집계 조회를 추가한다.
  3. 새 DB 컬럼·테이블이 꼭 필요한 경우에만 별도 승인을 받는다.
- DB 스키마를 변경하게 되면 변경 직후 전체 스키마를 재조회하고 `docs/guide_db.md`를 같은 작업에서 갱신한다.
- 새 컴포넌트/함수는 기존 책임으로 해결할 수 없는 경우에만 만들고, 변경 코드의 주석은 비개발자도 이해할 수 있는 한국어로 작성한다.

## 6. 구현 순서와 검증 기준

### 6-1. 결정 반영 및 공통 셸

- 아래 질문에 대한 답변을 반영해 데스크톱 내비게이션, breakpoint, 최대 폭, main/aside 비율을 확정한다.
- 공통 컨테이너·헤더·데스크톱 내비게이션·모바일 하단 메뉴의 중복 스타일을 정리한다.

검증: 1280px 이상에서 주요 페이지가 중앙의 좁은 카드 하나로 축소되지 않고, 390px 전후 모바일에서 가로 overflow와 하단 메뉴 충돌이 없다.

### 6-2. 업무 우선 화면

- `/` 구매장부와 `/menu-4` 자동추천에 요약·주의·빠른 액션을 먼저 배치한다.
- 기존 목록 동작, 후보 계산, 완료처리, 삭제, 익스텐션 다운로드를 보존한다.

검증: 사용자가 첫 화면에서 미완료/확인 필요 건수와 다음 처리 위치를 알 수 있고, 기존 처리 동작이 동일하다.

### 6-3. 분석 화면

- `/dashboard`와 `/dashboard/monthly`에 기간 비교, 월 이동, 확인할 주문, 분류별 요약을 배치한다.
- 월별 표와 상세 화면 사이 이동을 연결한다.

검증: 기간을 바꿨을 때 모든 KPI·비교·인사이트가 같은 범위를 사용하고, 주문이 없는 기간에도 빈 상태가 깨지지 않는다.

### 6-4. 입력·설정 화면

- 주문 추가/상세의 요약 패널과 설정/템플릿의 데스크톱 2단 레이아웃을 적용한다.
- 모바일에서는 입력 순서와 저장·완료 액션의 접근성을 다시 확인한다.

검증: 입력값과 우측 요약이 불일치하지 않고, 여러 구매계정 주문 생성·완료처리·템플릿 저장이 기존 규칙대로 동작한다.

### 6-5. 인증·공통 상태 및 최종 검증

- 로그인, 로딩, 오류, 빈 상태, 계정 메뉴를 넓은 셸과 모바일 셸에서 확인한다.
- `npm run lint`, `npm run build`를 실행한다.
- 최소한 다음 폭에서 주요 경로를 수동 점검한다: 390px, 768px, 1024px, 1280px, 1440px.
- 주요 경로: `/`, `/dashboard`, `/dashboard/monthly`, `/menu-4`, `/orders/new`, `/orders/detail`, `/settings`, 템플릿 추가/수정, `/login`.
- `git diff`로 DB·기존 업무 동작·크롬 익스텐션 파일에 불필요한 변경이 없는지 확인한다.

최종 완료 기준:

- PC에서 주요 업무 콘텐츠가 화면 폭에 맞게 확장되고, 확장된 공간에 사용자에게 의미 있는 요약·주의·빠른 액션이 있다.
- 모바일에서 주요 정보와 완료/저장/이동 액션이 가려지지 않는다.
- 새로운 정보가 기존 데이터와 모순되지 않고, 로딩·빈 데이터·오류 상태가 명확하다.
- 기존 DB 스키마를 변경하지 않았다면 `docs/guide_db.md` 변경은 없다. 스키마 변경을 선택했다면 해당 문서를 갱신한다.

## 7. 결정이 필요한 질문

아래는 구현 전에 답이 필요한 항목이다. 추천안은 기본값일 뿐이며, 답변에 따라 계획을 좁힌다.

### Q1. 데스크톱 내비게이션을 어떻게 할까요?

- **A (추천)**: `1024px` 이상에서 왼쪽 고정 사이드바, 모바일에서는 현재 하단 메뉴 유지.
- B: 사이드바를 추가하지 않고 현재 하단 메뉴를 모든 화면 폭에서 유지하며 콘텐츠만 넓힘.
- C: 사이드바 대신 상단 가로 내비게이션을 추가.

사이드바를 선택하면 PC 공간 활용과 페이지 이동성은 좋아지지만, 공통 셸 변경 범위가 커진다.


### Q2. PC 콘텐츠의 최대 폭을 어디까지 허용할까요?

- **A (추천)**: 1280~1440px의 유동 폭. 표·카드가 필요한 만큼만 넓어지게 함.
- B: 최대 1280px로 제한해 시선 이동을 줄임.
- C: 최대 1600px까지 사용해 대형 모니터의 표·보조 패널을 적극 활용.

답변: A

### Q3. PC에서 가장 중요한 사용자 업무는 무엇인가요?

- **A (추천)**: 미완료 주문·입금 매칭·자동추천 확인을 빨리 처리하는 운영 중심.
- B: 구매금액·입금금액·수익·회수 현황을 분석하는 재무 중심.
- C: AI 리뷰·카톡 정보·템플릿 작성 같은 콘텐츠 생성 중심.

이 답변이 구매장부와 각 보조 패널의 우선순위를 결정한다.

### Q4. 추가 정보는 어떤 데이터 범위까지 허용할까요?

- **A (추천)**: 현재 조회하는 DB 필드와 그로부터 계산되는 정보만 사용. 1차 DB 변경 없음.
- B: 화면 성능을 위해 기존 테이블의 count/sum 집계 쿼리까지 추가.
- C: 알림·작업 이력·사용량까지 저장할 새 테이블/컬럼을 추가.

C를 선택하면 DB 마이그레이션과 `docs/guide_db.md` 갱신이 별도 범위로 들어간다.

### Q5. 자동추천 PC 화면을 어떻게 보여줄까요?

- **A (추천)**: PC에서는 상단 탭/세그먼트로 주문 추천과 입금 추천을 전환하고, 현재 페이지 하나만 마운트.
- B: PC에서는 주문 추천과 입금 추천을 좌우 2열로 동시에 표시.
- C: 모바일과 PC 모두 현재 좌우 화살표·스와이프 슬라이더를 유지.

B는 PC 폭을 가장 많이 사용하지만 두 데이터 세트를 동시에 조회·렌더링할 수 있다.

### Q6. 구매장부의 우측 보조 영역은 무엇을 우선할까요?

- **A (추천)**: 오늘 확인할 일 + 빠른 작업 + 처리 우선순위.
- B: 최근 주문/최근 완료 내역과 활동 흐름.
- C: 플랫폼·구매계정별 금액 요약.

현재 별도 활동 로그는 없으므로 B는 주문의 `created_at`·상태만으로 구성하고, 실제 이벤트 이력은 표현하지 않는다.

### Q7. 주문 추가·상세 화면은 어떤 밀도로 만들까요?

- **A (추천)**: PC 2열 폼 + 우측 실시간 요약/상태 패널, 모바일 1열.
- B: 폼 자체만 1200px까지 넓히고 보조 패널은 추가하지 않음.
- C: 입력 영역과 AI/완료정보를 탭으로 분리.

A는 현재 폼을 재사용하면서 PC 공간과 입력 확인성을 동시에 높이는 안이다.

### Q8. 설정 화면의 PC 구조는 무엇이 좋을까요?

- **A (추천)**: 왼쪽 설정 메뉴를 계속 보이고 오른쪽에 선택 화면을 표시.
- B: 설정 홈을 2~3열 카드 허브로 만들고 선택하면 현재처럼 하위 화면으로 이동.
- C: 모바일과 동일한 드릴다운을 PC에서도 유지.

### Q9. 템플릿 입력 화면에 미리보기를 추가할까요?

- **A (추천)**: PC 우측에 카톡 붙여넣기 결과 실시간 미리보기, 모바일에서는 폼 아래.
- B: 입력 폼만 넓히고 미리보기는 추가하지 않음.
- C: 미리보기는 `복사하기`를 눌렀을 때만 모달로 표시.

### Q10. 분석 화면에 그래프를 어느 정도 사용할까요?

- **A (추천)**: 기존 표·수치와 CSS 막대/증감 배지만 사용하고 차트 라이브러리는 추가하지 않음.
- B: 기존 의존성으로 가능한 간단한 그래프만 추가.
- C: 차트 라이브러리를 새로 도입해 추이·분포 차트를 적극적으로 추가.

### Q11. 현재 동작하지 않는 알림 아이콘은 어떻게 할까요?

- **A (추천)**: 이번 작업에서 미완료/확인 필요 요약으로 연결되는 알림 패널을 구현.
- B: 실제 알림 데이터가 없으므로 아이콘을 제거.
- C: 알림 센터와 읽음 상태를 새 DB 구조로 설계.

C는 별도 제품 기능과 DB 범위가 필요하다.

### Q12. 로그인 화면도 PC 정보 밀도 개선 대상에 포함할까요?

- **A (추천)**: 포함하되 split layout은 로그인 전용으로만 적용.
- B: 인증 화면은 집중을 위해 현재 중앙 카드 구조를 유지.
- C: 로그인과 비로그인 홈 모두 제품 소개형 랜딩 화면으로 확장.

### Q13. 자동추천의 크롬 익스텐션 안내는 어디에 둘까요?

- **A (추천)**: PC에서는 자동추천 보조 패널, 모바일에서는 현재 목록 위 전체 폭.
- B: 현재처럼 두 화면 모두 목록 위 전체 폭.
- C: 설정에 별도 “연동/익스텐션” 메뉴를 추가하고 자동추천에서는 짧은 링크만 표시.

### Q14. 새로운 요약 수치의 집계 시점을 어떻게 할까요?

- **A (추천)**: 첫 화면은 빠르게 열고, 상세 금액 집계는 비동기로 채움. 기존 lazy loading 유지.
- B: 모든 요약 데이터가 준비될 때까지 화면을 기다림.
- C: 별도 서버 집계/API를 구축.

### Q15. 주문 이력/알림 데이터가 필요할까요?

- **A (추천)**: 이번에는 현재 행의 생성일·수정일·상태만 표시하고 새 이력은 만들지 않음.
- B: 주문 상태 변경·입금 매칭·삭제 등의 이벤트 이력을 추가.
- C: 이력과 알림을 함께 새 도메인 기능으로 설계.

### Q16. 이번 계획의 구현 범위를 어디까지로 확정할까요?

- **A (추천)**: 위에 적은 모든 라우트와 설정 하위 뷰를 대상으로 하되, 핵심은 `/`, `/dashboard`, `/dashboard/monthly`, `/menu-4`, 주문 입력/상세, 설정.
- B: 업무 핵심 5개 화면(`/`, `/dashboard`, `/dashboard/monthly`, `/menu-4`, 주문 상세)만 먼저 구현.
- C: 공통 셸과 PC 폭 조정만 먼저 하고 정보 추가는 다음 단계로 분리.

## 8. 답변 반영 기록

최종 선택을 아래에 기록했다. 선택하지 않은 제안은 구현 범위에서 제거한다.

- Q1: A
- Q2: A
- Q3: A
- Q4: B
- Q5: A
- Q6: A
- Q7: A
- Q8: A (설정 메뉴를 상시 표시) → 후속 구체화: 글로벌 사이드바의 설정 하위 메뉴로 통합
- Q9: A
- Q10: C (새 차트 라이브러리 도입) → 후속 확정: Recharts 사용
- Q11: B
- Q12: C (로그인과 비로그인 홈 모두 랜딩화) → 후속 확정: `/login`과 비로그인 `/`가 같은 소개형 로그인 흐름을 공유하고, 로그인 후 `/`는 구매장부로 전환
- Q13: A
- Q14: A
- Q15: A
- Q16: A

### 후속 질문 답변

- 차트 라이브러리: **A — Recharts**
- 비로그인 랜딩 흐름: **A — `/login`과 비로그인 `/`가 같은 소개형 랜딩 + 로그인 카드를 공유하고, 로그인 후 `/`는 구매장부로 사용**
- 글로벌 사이드바와 설정 메뉴: **B — 글로벌 사이드바의 설정 항목을 펼쳐 하위 메뉴를 보여주고 설정 내부의 별도 사이드바는 제거**

---

# 2차 사용자 편의 기능 및 구조 리팩터링 계획

작성일: 2026-08-08<br>
상태: 2A 구현·검증 완료 / 2B 구현 대기 (2026-08-08)

## 1. 2차 목표와 기본 판단

1차 작업에서 PC 화면 폭, 글로벌 내비게이션, 운영 요약, 차트와 보조 패널을 확보했다. 2차는 화면을 더 추가하는 것보다 사용자가 매일 반복하는 입력·검색·처리 횟수를 줄이고, 잘못 누른 작업을 복구할 수 있게 만드는 것이 우선이다.

이번 분석에서는 다음을 기본 전제로 둔다.

- 서비스는 Android 앱 의존성 없이 데스크톱·모바일 브라우저에서 동일하게 사용한다.
- 핵심 사용자는 주문을 직접 등록하고 입금·배송·리뷰 상태를 정리하는 운영자다.
- 자동화는 사용자의 확인 없이 주문 상태나 금액을 바꾸지 않는다.
- 현재 DB 필드로 만들 수 있는 기능을 먼저 구현하고, 동기화·휴지통·알림 이력처럼 DB가 꼭 필요한 기능은 별도 단계로 구분한다.
- 완료 주문 지연 조회, 가상 스크롤, 자동추천 페이지별 지연 로딩은 유지한다.
- 2차 리팩터링은 파일을 임의로 잘게 나누기 위한 작업이 아니라, 새 기능에서 완료처리·검색·저장 로직이 중복되지 않게 만드는 범위로 제한한다.

### 확정 결론

가장 체감이 큰 기본 묶음은 아래 순서다.

1. **주문 재사용**: 주문 복제, 임시저장, 최근 입력값 기본 선택, 저장 전 중복 경고
2. **빠른 찾기**: 원클릭 빠른 필터, URL로 유지되는 필터, 전체 검색 `Ctrl/Cmd + K`
3. **대기열 처리**: 안전한 일괄 수정, 자동추천 처리 후 다음 항목 자동 이동
4. **실수 복구**: 숨김 취소, 휴지통, 실행 취소
5. **일정 활용**: 기존 구매 예정 시각을 폼·장부·오늘 할 일에 연결하는 인앱 일정

엑셀 가져오기, 설치형 PWA, 브라우저 푸시, 리뷰 마감일, 활동 기록, 별도 입금계좌 설정 화면은 이번 2차 범위에서 제외한다.

## 2. 현재 구조에서 확인한 사용자 불편과 리팩터링 근거

| 영역 | 현재 상태 | 사용자 불편 또는 위험 | 2차 방향 |
| --- | --- | --- | --- |
| 구매장부 | 미완료·완료별 검색어와 날짜 필터가 있으며 화면을 벗어나면 초기화됨 | 자주 쓰는 조건을 매번 다시 입력하고, 요약 카드에서 해당 주문으로 바로 좁힐 수 없음 | 빠른 필터·저장 보기·URL 필터 |
| 주문 등록 | 새 주문은 매번 폼을 처음부터 입력 | 같은 카톡방·플랫폼·계정 주문을 반복 등록할 때 입력량이 큼 | 복제·최근값·초안 자동저장 |
| 주문 저장 | 주문번호·구매일·금액이 비슷해도 별도 중복 확인 없음 | 크롤링 주문과 수동 주문이 중복 등록될 가능성 | 저장 전 중복 후보 경고 |
| 주문 처리 | 한 건씩 배송/완료/마스터 정보를 수정 | 같은 조건의 주문이 많을 때 반복 클릭이 많음 | 선택 모드·일괄 수정·처리 전 미리보기 |
| 삭제 | `orders`는 확인 후 바로 실제 삭제 | 실수로 삭제하면 화면에서 복구할 방법이 없음 | 휴지통 또는 지연 삭제 |
| 자동추천 | 추천 건을 처리한 뒤 다시 목록에서 다음 건을 선택 | 연속 검수 시 이동 동작이 반복됨 | 처리 후 다음 항목 자동 열기 |
| 자동추천 숨김 | `crawl_order_status = 99`, `bank_account_deposit_status = 99`로 숨김 | 잘못 숨긴 항목을 되돌리는 화면이 없음 | 최근 처리·숨김 항목 복구 보기 |
| 대시보드 | 엑셀 전체 내보내기와 월 상세 이동은 가능 | KPI·차트에서 실제 주문 목록으로 바로 이동하지 못하고, 선택 기간만 내보낼 수 없음 | 장부 드릴다운·현재 범위 내보내기 |
| 설정/템플릿 | 템플릿 추가·수정·복사는 가능 | 복제·기본 템플릿·삭제·사용량 확인이 없음 | 템플릿 빠른 관리·기본값 설정 |
| 데이터 이동 | 전체 엑셀 내보내기만 존재 | 필요한 범위만 내보내려면 후처리가 필요함 | 현재 기간·필터·선택 주문 내보내기만 확장하고 가져오기는 제외 |
| 웹 전용 사용 | 브라우저 접속만 지원 | 별도 설치 없이 브라우저에서 사용함 | 현재 웹 서비스 형태를 유지하고 PWA는 제외 |

구조적으로는 아래 파일에 여러 책임이 모여 있다.

- `orders-table.tsx`: 약 2,160줄 — 필터, 가상 스크롤, 모바일/PC 행, 완료/미완료 처리, 삭제가 함께 있음
- `crawl-orders-page.tsx`: 약 1,867줄 — 크롤링 실행, 데이터 조회, 추천 계산, 주문/입금 화면이 함께 있음
- `order-detail-form.tsx`: 약 1,527줄 — 폼 상태, 검증, 저장, 이탈 방지, 템플릿, AI 리뷰가 함께 있음
- `settings-panel.tsx`: 약 1,112줄 — 모든 설정 하위 화면과 변경 로직이 함께 있음
- 주문 relation 조회 문자열과 원화·날짜 포맷 로직이 여러 페이지에 반복됨
- 모바일과 PC의 주문 완료처리가 같은 규칙을 각각 구현하고 있어 일괄 처리 추가 시 규칙이 어긋날 위험이 있음
- 현재 자동추천 알고리즘·완료 금액 계산·필터 로직을 보호할 자동 테스트가 없음

따라서 기능 추가 전에 공통 규칙을 먼저 분리하되, 화면별 JSX를 전부 재작성하는 대규모 리팩터링은 하지 않는다.

## 3. 확정 기능 우선순위

| 우선순위 | 기능 | 사용자 효과 | DB 영향 | 예상 난이도 |
| --- | --- | --- | --- | --- |
| P0 | 주문 복제·이전 주문으로 새 주문 만들기 | 반복 입력을 가장 크게 줄임 | 없음 | 중 |
| P0 | 주문 초안 자동저장·복구 | 탭 종료·새로고침 시 입력 손실 방지 | 사용자별 DB 동기화 필요 | 중 |
| P0 | 빠른 필터·저장 보기·URL 필터 | 미완료/미배송/일정 주문을 한 번에 찾음 | 저장 보기 DB 동기화 필요 | 중 |
| P0 | 자동추천 처리 후 다음 항목 자동 열기 | 연속 검수의 화면 이동 횟수 감소 | 없음 | 중 |
| P0 | 저장 전 중복 주문 감지 | 수동·크롤링 중복 등록 방지 | 기존 `orders` 조회만 추가 | 중 |
| P1 | 선택 모드·안전한 일괄 수정 | 여러 주문의 배송·분류를 한 번에 정리 | 스키마 변경 없음, 원자 처리 RPC는 선택 | 상 |
| P1 | 전체 검색·명령 팔레트 | 어느 화면에서든 주문/메뉴/템플릿 이동 | 검색 쿼리 추가 | 상 |
| P1 | 템플릿 복제·기본값·삭제 | 카톡 정보 재사용 속도 향상 | 기본값 DB 동기화 필요 | 중 |
| P1 | 대시보드에서 장부 드릴다운 | 숫자의 원인 주문을 바로 확인 | 없음 | 중 |
| P1 | 현재 필터/기간만 엑셀 내보내기 | 필요한 자료만 전달 가능 | 없음 | 하 |
| P2 | 휴지통·실행 취소 | 실수로 삭제한 주문 복구 | `orders.deleted_at` 추가 | 상 |
| P2 | 숨김/처리된 자동추천 복구 보기 | 잘못 숨긴 크롤링·입금 항목 복구 | 기존 status 사용 가능 | 중 |
| P2 | 구매 예정 일정 대기열 | `scheduled_purchase_at`을 실제 업무에 활용 | 기존 필드로 가능 | 중 |
| 제외 | 엑셀 가져오기 | 현재 내보내기 흐름에 집중 | 구현하지 않음 | - |
| 제외 | 리뷰 마감일·브라우저 알림 | 기존 구매 일정의 인앱 표시만 사용 | 구현하지 않음 | - |
| 제외 | 설치형 PWA | 현재 브라우저 서비스 형태 유지 | 구현하지 않음 | - |
| 제외 | 별도 입금계좌 설정 화면 | 자동추천의 현재 표시만 유지 | 구현하지 않음 | - |

## 4. 화면별 2차 기능 상세

### 4-1. `/` 구매장부: 빠른 보기와 선택 모드

#### 빠른 필터

상단 요약 카드와 `오늘 확인할 일`을 실제 필터 동작에 연결한다.

- `입금 미완료`: `is_processed = false`
- `미배송`: `is_item_delivered = false`
- `구매 일정 지남`: `scheduled_purchase_at <= 현재 시각`
- `입금 정보 확인`: 완료됐지만 입금일·입금금액이 비어 있는 주문
- `AI 리뷰 없음`: 리뷰 조건은 있으나 `ai_review`가 비어 있는 주문
- `템플릿 미연결`: 카톡 복사가 필요하지만 템플릿이 없는 주문
- `이번 주`, `이번 달`, `최근 30일` 날짜 프리셋

필터 상태는 `?status=pending&delivery=undelivered&from=...` 형태로 URL에 반영해 새로고침과 공유 후에도 유지한다. 이름 붙인 개인 저장 보기는 사용자별 DB에 저장해 PC와 모바일에서 동기화한다.

#### 선택 모드와 일괄 작업

- PC: 행 체크박스 또는 `선택` 버튼으로 선택 모드 진입
- 모바일: 행 길게 누르기 또는 명시적인 `선택` 버튼 사용
- 선택 시 상단 또는 하단에 sticky 작업 막대 표시
- 안전한 기본 일괄 작업:
  - 배송/미배송 변경
  - 플랫폼·결제수단·구매계정 변경
  - 템플릿 연결/해제
  - 엑셀 선택 내보내기
- 일괄 완료처리는 입금일·입금금액·배송 경고가 건마다 다르므로 별도의 단계형 확인 화면으로 둔다.
- 작업 전 `변경 대상 N건`, 변경 전/후 값을 보여주고 최종 확인 뒤 실행한다.
- 중간 실패가 있으면 성공·실패 건을 나눠 보여주고 실패 주문만 다시 시도할 수 있게 한다.

#### 행 빠른 동작

- `같은 정보로 새 주문 만들기`
- 상품 URL 열기
- 주문번호 복사
- 연결된 카톡 템플릿 복사
- AI 리뷰 복사
- 완료/미완료 전환

모바일에서도 hover 없이 같은 기능에 접근할 수 있어야 하며, 스와이프 삭제와 선택 모드가 충돌하지 않도록 선택 모드에서는 스와이프 삭제를 끈다.

### 4-2. `/orders/new`, `/orders/detail`: 반복 입력 최소화

#### 주문 복제

주문 상세과 장부 행 메뉴에 `같은 정보로 새 주문 만들기`를 추가한다. 추천 기본 규칙은 다음과 같다.

- 유지: 카톡방 이름, 상품명, 구매가격, 플랫폼, 결제수단, 구매계정, 배송 여부, 템플릿, 리뷰 사진/글자 수
- 초기화: 주문 ID, 주문번호, 입금일, 입금금액, 수익, 입금메모, 완료 상태, AI 리뷰 결과
- 구매일: 한국 시간 오늘
- 메모·상품 URL·구매 예정 시각은 사용자가 복제 확인창에서 포함 여부를 선택

복제는 기존 `OrderDetailForm`의 초기값 경로를 재사용하고 DB 복사 함수를 별도로 만들지 않는다.

#### 초안 자동저장과 최근 입력값

- 새 주문 폼의 변경값을 짧은 debounce 후 사용자별 DB 초안으로 저장해 PC와 모바일에서 이어서 작성할 수 있게 한다.
- 다시 들어오면 `작성 중이던 주문이 있습니다` 배너에서 복구 또는 폐기할 수 있다.
- 저장 완료 후 해당 초안만 제거한다.
- 템플릿의 연락처·주소·계좌처럼 민감할 수 있는 원문은 초안에 저장하지 않는다.
- 플랫폼·결제수단·구매계정·템플릿은 최근 선택값을 다음 신규 주문의 기본값으로 제안한다.
- 최근 선택값과 사용자 기본값은 사용자별 DB에 동기화한다. 기본값은 폼 상단에 `기본값 적용됨`을 표시하고 한 번에 초기화할 수 있게 한다.

#### 저장 전 중복 감지

- 주문번호가 같으면 강한 중복 경고
- 주문번호가 없을 때는 구매일, 구매계정, 구매가격, 상품명/카톡방 이름 유사도를 조합
- 중복 후보를 최대 3건 보여주고 `기존 주문 열기`, `그래도 저장`을 선택
- 경고만 제공하고 사용자가 확인하면 저장을 허용한다.
- 다중 구매계정 주문 생성 시 생성 대상별로 중복 여부를 표시한다.

#### 저장 후 다음 동작

- `저장 후 장부로 이동`
- `저장 후 같은 정보로 계속 추가`
- `저장 후 새 빈 주문 추가`

기본 동작은 설정에서 정하거나 최근 선택을 기억한다.

#### 접힌 추가 정보

현재 DB에 이미 있는 `notes`, `product_url`, `scheduled_purchase_at`, `order_status`를 `추가 정보` 접힘 영역에서 직접 입력·수정할 수 있게 한다. 구매 예정 시각을 입력하면 구매장부의 일정 필터와 오늘 할 일에 연결한다.

### 4-3. `/menu-4` 자동추천: 연속 처리 모드

- 검수 저장·삭제·매핑완료 뒤 목록으로만 돌아가지 않고 다음 대기 항목을 자동으로 연다.
- `처리 후 다음 항목 열기` 토글을 제공하고 사용자의 마지막 선택을 기억한다.
- 진행 상태를 `처리한 건수 / 남은 건수`로 표시한다.
- PC 키보드 동작:
  - `J`/`K`: 다음/이전 후보
  - `Enter`: 현재 항목 상세 열기
  - `Esc`: 목록으로 돌아가기
  - 상태 변경 단축키는 오작동 위험 때문에 두 번 확인 없이 제공하지 않는다.
- 주문 추천 필터: 플랫폼 계정, 구매일, 자동 매칭/확인 필요
- 입금 추천 필터: 입금 계좌, 날짜, 금액 범위, 추천 후보 있음/없음
- 100% 제목 일치와 계좌주·금액까지 일치하는 경우 `강한 추천`으로 표시하되 자동 완료하지 않는다.
- 최근 처리·숨김 탭에서 status `1` 또는 `99` 항목을 제한된 기간만 조회하고, 잘못 숨긴 항목은 status `0`으로 복구할 수 있게 한다.
- 크롤링 실행 시각·마지막 성공 계정·실패 계정을 헤더에서 확인할 수 있게 한다. 영구 이력이 필요하면 별도 DB가 필요하다.

### 4-4. 전체 검색과 명령 팔레트

데스크톱 사이드바와 모바일 헤더에서 `Ctrl/Cmd + K` 전체 검색을 연다.

- 메뉴 이동: 구매장부, 주문 추가, 자동추천, 대시보드, 설정 하위 화면
- 주문 검색: 상품명, 카톡방 이름, 주문번호, 메모
- 템플릿 검색: 제목
- 빠른 동작: 새 주문, 현재 화면 새로고침, 대시보드 엑셀 내보내기
- 주문 결과에는 완료/미완료, 배송 상태, 구매일, 금액을 표시
- 입력 debounce 후 상위 20건만 조회하고 더 보기는 구매장부 필터 화면으로 이동
- 완료 주문 전체를 첫 화면에서 불러오지 않고 검색어가 있을 때만 Supabase 검색 쿼리를 실행
- 검색어와 주문 ID는 URL로 넘기고, 개인정보가 포함될 수 있는 검색 기록은 기본적으로 저장하지 않는다.

### 4-5. `/dashboard`, `/dashboard/monthly`: 숫자에서 실제 주문으로 이동

- KPI 카드, 차트 범례, 분류별 행을 클릭하면 구매장부를 해당 기간·상태·플랫폼·계정 필터로 연다.
- `미회수 원금`은 미완료 주문, `미배송`은 배송 상태 필터로 연결한다.
- 월별 상세의 `확인할 항목`도 필터된 장부로 연결한다.
- 엑셀 내보내기를 `전체`, `현재 기간`, `현재 필터`, `선택 주문`으로 나눈다.
- 차트 종류를 더 늘리기보다 각 수치가 실제 업무로 이어지는 드릴다운을 우선한다.

### 4-6. `/settings`와 구매 정보 템플릿

#### 사용자 기본값

- 기본 플랫폼
- 기본 결제수단
- 기본 구매계정
- 기본 구매 정보 템플릿
- 주문 저장 후 이동 방식
- 자동추천 처리 후 다음 항목 자동 열기 여부
- 장부 기본 보기: 미완료만 / 마지막 필터 복원 / 전체

사용자 기본값은 PC와 모바일에서 같게 보이도록 사용자별 DB에 저장한다. 구현 전에 기존 사용자 설정 저장 책임을 재사용할 수 있는지 확인하고, 없을 때만 목적이 분명한 설정 테이블을 추가한다.

#### 템플릿 빠른 관리

- 기존 템플릿 복제
- 기본 템플릿 지정
- 사용 중인 주문 수 표시
- 삭제 전 연결된 주문 수 경고
- 템플릿 삭제 시 기존 주문의 FK는 현재 규칙대로 `NULL`이 되며, 사용자는 삭제 영향을 확인해야 한다.
- 목록에는 입력된 필드 수와 최근 수정일을 표시하되 연락처·주소·계좌번호 원문은 기본 목록에서 숨긴다.

#### 마스터 데이터 안전성

- 플랫폼·결제수단·구매계정마다 연결된 주문 수를 표시한다.
- 사용 중인 사용자 항목 삭제 시 `N건의 주문에서 미지정으로 바뀔 수 있음`을 알려준다.
- 이름 변경 기능을 추가한다면 중복 이름 제약을 저장 전에 검사한다.

### 4-7. 엑셀 내보내기

- 기존 전체 엑셀 내보내기는 유지한다.
- 대시보드의 현재 기간, 구매장부의 현재 필터, 사용자가 선택한 주문만 내보내는 범위를 추가한다.
- 화면에 보이는 정렬·필터 조건과 파일의 데이터 범위가 같다는 것을 내보내기 전에 표시한다.
- 엑셀 가져오기 UI, 행 검증, 대량 insert, 전용 RPC는 구현하지 않는다.

### 4-8. 실수 복구와 활동 기록

#### 휴지통 확정안

- `orders.deleted_at timestamptz NULL`을 추가해 목록 삭제는 soft delete로 변경
- 기본 목록은 `deleted_at IS NULL`만 조회
- 설정의 휴지통에서 최근 삭제 주문을 복구하거나 영구 삭제
- 삭제 직후 토스트에 `실행 취소` 제공

DB 변경 없이 브라우저에서 몇 초 뒤 실제 삭제하는 방식은 새로고침·탭 종료 시 복구가 보장되지 않아 권장하지 않는다.

#### 활동 기록(2차 범위 제외)

- 주문 생성, 수정, 완료/미완료, 입금 매핑, 삭제/복구 이벤트를 기록
- `누가`, `언제`, `무엇을`, `이전/이후 상태`를 저장
- 현재는 단일 사용자 중심이므로 휴지통보다 우선순위가 낮고 2차에서는 구현하지 않는다.
- 향후 필요해지면 별도 `order_events` 테이블과 보관 기간 정책을 먼저 결정한다.

### 4-9. 구매 예정 인앱 일정

- 기존 `scheduled_purchase_at`을 주문 추가·상세의 `추가 정보`에서 입력하고 수정할 수 있게 한다.
- 구매장부에 오늘, 지연, 다가오는 구매 일정 필터와 상태 배지를 제공한다.
- `오늘 확인할 일`에서 일정 건수를 누르면 해당 조건으로 장부를 바로 좁힌다.
- 별도 일정 DB, 리뷰 마감일, 브라우저 권한 요청, Web Push, 서비스 워커, PWA manifest는 추가하지 않는다.

### 4-10. 입금계좌 설정 화면 제외

`bank_account`에는 계좌번호 외에 `bank_password`, `resident_number`가 포함돼 있다. 편의를 이유로 이 값을 브라우저 설정 화면에 그대로 조회·수정하는 기능은 추가하지 않는다.

- 자동추천에서 현재 제공하는 계좌 표시만 유지하고 별도 설정 화면은 만들지 않는다.
- 계좌 추가·수정·수집 상태 관리 UI와 자격정보 조회 기능은 구현하지 않는다.
- 향후 자격정보 변경이 필요하면 Edge Function 또는 별도 서버에서만 처리하고 클라이언트 응답에는 원문을 포함하지 않는 보안 구조를 먼저 확정한다.

## 5. 기능 추가를 위한 리팩터링 계획

### 5-1. 주문 타입과 조회 정의 이동

현재 여러 `lib`과 페이지가 `orders-table.tsx`에서 `OrderWithRelations` 타입을 가져온다. UI 컴포넌트가 도메인 타입의 소유자가 되지 않도록 주문 relation 타입과 공통 select 정의를 `src/types` 또는 기존 주문 공통 모듈로 이동한다.

- 대시보드·월별 상세·주문 상세의 동일 relation select 문자열 통합
- 목록에 필요한 최소 컬럼과 상세에 필요한 전체 컬럼은 구분
- 새 파일은 이 공통 책임이 기존 파일에 들어갈 적절한 위치가 없을 때만 만든다.

### 5-2. 완료처리 규칙 통합

모바일·PC·입금추천·상세 폼의 완료처리에서 아래 규칙을 공통화한다.

- 입금일/입금금액 검증
- 수익 계산
- 배송 여부와 입금액 불일치 경고
- Supabase update payload
- 성공 후 목록 상태 반영

일괄 완료처리도 같은 공통 함수로 건별 미리보기 결과를 만들게 한다.

### 5-3. 큰 컴포넌트의 책임 분리

- `OrdersTable`: 필터 상태, 미완료 섹션, 완료 섹션, 행 액션, 선택 막대를 분리
- `OrderDetailForm`: 폼 상태/검증은 한 곳에 유지하고 구매정보·완료정보·AI·템플릿 섹션만 표시 컴포넌트로 분리
- `CrawlOrdersPage`: 순수 추천 계산, 데이터 로딩, 크롤링 실행, 주문 추천 화면, 입금 추천 화면을 분리
- `SettingsPanel`: 설정 view별 컴포넌트를 분리하되 마스터 변경 공통 함수는 공유

분리 후에도 페이지마다 새로운 상태 저장소를 만들지 않고, 부모가 필요한 상태만 소유한다.

### 5-4. 날짜·금액·필터 공통 로직

- 한국 시간 오늘 날짜 계산 통합
- 원화·날짜 포맷 통합
- 구매기간·완료·배송·마스터 필터의 URL 직렬화/역직렬화 통합
- 필터가 잘못된 URL 값을 받아도 안전한 기본값으로 복구

### 5-5. 테스트 기반 안전망

현재 별도 테스트 스크립트가 없으므로 리팩터링 전에 최소 테스트 환경을 추가한다.

- 단위 테스트: 수익 계산, 배송/입금 경고, 중복 점수, 자동추천 유사도, URL 필터 변환
- 컴포넌트 테스트: 주문 복제 초기값, 초안 복구 배너, 일괄 작업 미리보기
- 브라우저 핵심 흐름: 로그인 후 장부 열기, 주문 추가, 완료처리, 자동추천 탭, 설정 이동
- 기존 `npm run lint`, `npm run build`는 계속 필수

테스트 프레임워크는 구현 시작 시 현재 Next.js 16 문서를 다시 확인한 뒤 프로젝트에 가장 작은 구성으로 추가한다.

### 5-6. 민감정보와 대량 변경 경계

- 주문 초안에 주소·연락처·계좌 원문을 저장하지 않는다.
- 전체 검색 결과에 템플릿 민감 필드를 포함하지 않는다.
- 일괄 변경은 사용자 ID/RLS 조건과 대상 ID를 함께 검증한다.
- 원자성이 꼭 필요한 일괄 작업만 RPC를 사용하고, 일반 단건 수정까지 불필요한 서버 추상화를 만들지 않는다.

## 6. 데이터 변경 분류

### DB 변경 없이 가능한 기능

- 주문 복제
- 빠른 필터와 URL 필터
- 저장 전 중복 조회
- 자동추천 다음 항목 이동·키보드 탐색
- status 기반 숨김 항목 복구
- 대시보드 드릴다운
- 현재 기간·선택 주문 엑셀 내보내기
- 템플릿 복제·삭제
- 기존 구매 예정 시각 기반 인앱 일정

### 사용자 선택으로 DB 변경이 필요한 기능

| 기능 | 예상 변경 |
| --- | --- |
| 여러 기기 초안·기본값 동기화 | 기존 설정 저장 책임을 우선 재사용하고, 없으면 사용자별 초안·설정 테이블 추가 |
| 저장 보기 동기화 | 사용자별 이름·필터 조건·정렬을 저장하는 테이블 추가 |
| 휴지통 | `orders.deleted_at`, 필요 시 인덱스 |
| 원자적 대량 변경 | 검증을 포함한 Supabase RPC |

DB 스키마를 변경하면 같은 작업에서 변경 후 전체 `public` 스키마를 다시 확인하고 `docs/guide_db.md`와 타입을 갱신한다.

## 7. 확정 구현 묶음과 순서

### 2A. 반복 입력·검색 개선 — 먼저 구현

1. 공통 주문 타입·조회·날짜/금액·완료처리 규칙 정리
2. 주문 복제와 저장 후 다음 동작
3. 새 주문 초안 자동저장·최근값
4. 저장 전 중복 감지
5. 빠른 필터와 URL 필터
6. 자동추천 처리 후 다음 항목
7. 대시보드 드릴다운·현재 범위 내보내기
8. 템플릿 복제·기본 템플릿
9. 기존 구매 예정 시각 기반 인앱 일정

사용자 선택(2차 Q3 B, Q4 C)에 따라 2A에서 초안·기본값·저장 보기의 여러 기기 동기화를 위한 DB 변경을 포함한다.

### 2B. 대량 처리·복구

1. 주문 선택 모드
2. 안전한 일괄 수정
3. 일괄 완료 단계형 확인
4. 최근 처리·숨김 자동추천 복구
5. 휴지통과 실행 취소

휴지통을 위해 `orders.deleted_at` DB 변경을 포함하며, 일괄 완료처리는 다른 일괄 수정이 안정화된 뒤 진행한다.

### 이번 2차 범위에서 제외

- 엑셀 가져오기
- 설치형 PWA
- 리뷰 마감일과 브라우저 푸시
- 활동 기록
- 별도 입금계좌 설정 화면

기존 구매 예정 시각을 활용한 인앱 일정은 2A에 포함하고, 2A 완료·검증 후 2B를 별도 작업으로 진행한다.

## 8. 검증 기준

- 기존 주문과 같은 주문을 만들 때 필수 수정만으로 30초 안에 등록할 수 있다.
- 장부 요약 카드에서 대상 주문 목록까지 한 번의 클릭으로 이동한다.
- 필터된 URL을 새로고침해도 같은 목록이 보인다.
- 10건 이상의 주문을 선택해 배송/분류를 한 번의 확인 흐름으로 바꿀 수 있다.
- 저장 전 중복 후보가 있으면 기존 주문을 먼저 열어볼 수 있다.
- 자동추천 한 건 처리 후 다음 대기 건으로 연속 이동할 수 있다.
- 삭제한 주문은 선택한 정책에 따라 복구할 수 있다.
- 모바일 선택 모드와 기존 스와이프 동작이 충돌하지 않는다.
- 완료 주문 lazy loading과 목록 가상 스크롤이 유지된다.
- 390px, 768px, 1024px, 1280px, 1440px에서 주요 액션이 가려지지 않는다.
- 단위 테스트, 핵심 브라우저 흐름, `npm run lint`, `npm run build`가 통과한다.

## 9. 2차 구현 결정 질문과 사용자 답변

### 2차 Q1. 가장 먼저 줄이고 싶은 불편은 무엇인가요?

- **A (추천)**: 반복 주문 입력과 원하는 주문 찾기
- B: 여러 주문의 상태를 한꺼번에 처리하기
- C: 일정·리뷰 알림을 놓치지 않기

### 2차 Q2. 주문 복제 시 기본 초기화 범위는 어떻게 할까요?

- **A (추천)**: 상품·플랫폼·계정·템플릿은 유지하고 주문번호·입금·완료·AI 리뷰는 초기화
- B: 구매일만 오늘로 바꾸고 나머지는 거의 그대로 복제
- C: 복제할 때마다 유지할 필드를 체크박스로 선택

### 2차 Q3. 초안과 사용자 기본값을 어디에 저장할까요?

- **A (추천)**: 우선 현재 브라우저에만 저장해 빠르게 구현
- B: PC와 모바일에서 같게 보이도록 DB에 동기화
- C: 초안은 로컬, 기본값·저장 필터만 DB 동기화

### 2차 Q4. 장부 필터는 어느 수준까지 저장할까요?

- **A (추천)**: URL 필터 + 브라우저에 이름 붙인 저장 보기
- B: URL 필터만 제공하고 저장 보기는 만들지 않음
- C: 저장 보기를 DB에 저장해 여러 기기에서 동기화

### 2차 Q5. 일괄 처리 범위는 어디까지 허용할까요?

- **A (추천)**: 배송·플랫폼·결제수단·구매계정·템플릿만 일괄 변경하고 완료처리는 별도 마법사
- B: 입금일과 금액을 한 번 입력해 선택 주문 전체를 바로 완료
- C: 일괄 처리는 제외하고 빠른 필터·복제만 구현

### 2차 Q6. 주문 삭제 복구 정책은 무엇이 좋을까요?

- **A (추천)**: DB에 `deleted_at`을 추가해 휴지통과 실행 취소 제공
- B: 삭제 후 몇 초 동안만 실행 취소하고 이후 실제 삭제
- C: 현재처럼 확인 후 즉시 삭제

### 2차 Q7. 자동추천 연속 처리 방식은 어떻게 할까요?

- **A (추천)**: 처리 후 다음 대기 항목을 자동으로 열고 최근 처리/숨김 복구 화면 제공
- B: 다음 항목 자동 이동만 제공
- C: 현재처럼 처리 후 목록으로 돌아감

### 2차 Q8. 일정 기능 범위는 어디까지 할까요?

- **A (추천)**: 기존 구매 예정 시각을 폼과 장부에 연결하고 인앱 일정만 제공
- B: 리뷰 마감일과 브라우저 푸시 알림까지 새 DB로 구현
- C: 일정 기능은 2차에서 제외

### 2차 Q9. 엑셀 가져오기가 필요한가요?

- **A (추천)**: 검증·중복 확인·미리보기가 있는 안전한 가져오기 추가
- B: 내보내기만 유지하고 가져오기는 보류
- C: 단순한 형식의 신규 주문 CSV 가져오기만 추가

### 2차 Q10. 템플릿 관리 범위는 어떻게 할까요?

- **A (추천)**: 복제·기본 지정·삭제·사용 주문 수까지 추가
- B: 복제와 기본 지정만 추가
- C: 현재 추가·수정·복사만 유지

### 2차 Q11. 웹 앱 설치 기능을 포함할까요?

- **A (추천)**: 홈 화면 설치와 독립 실행만 지원하는 기본 PWA
- B: PWA와 브라우저 푸시 알림을 함께 구현
- C: 이번에는 제외

### 2차 Q12. 입금계좌 관리 화면은 어떻게 할까요?

- **A (추천)**: 민감정보 없이 마스킹 계좌와 수집 상태만 읽기 전용 표시
- B: 서버 보안 구조부터 개선한 뒤 추가·수정까지 구현
- C: 자동추천에서 현재처럼 표시만 하고 별도 설정 화면은 만들지 않음

### 2차 Q13. 2차 구현 범위를 어떻게 나눌까요?

- **A (추천)**: 2A를 먼저 완료·검증한 뒤 2B를 별도 작업으로 진행
- B: 2A와 2B를 한 번에 구현
- C: 주문 복제·초안·빠른 필터·자동추천 다음 이동만 최소 구현

답변 예시: `2차 Q1 A, Q2 A, Q3 C ... Q13 A`

Q1: A,B
Q2: A
Q3: B
Q4: C
Q5: A
Q6: A
Q7: A
Q8: A
Q9: 구현안함
Q10: A
Q11: C
Q12: C
Q13: A

## 10. 2차 기능 답변 반영

사용자가 남긴 원문 답변은 위에 그대로 보존한다. 현재 문서의 질문과 매핑하면 다음과 같다.

| 질문 | 선택 | 계획 반영 |
| --- | --- | --- |
| 2차 Q1 | A, B | 반복 주문 입력·검색과 여러 주문 일괄 처리를 모두 2차 핵심으로 둔다. 구현 순서는 반복 입력·검색을 먼저 안정화하고 일괄 처리를 이어서 진행한다. |
| 2차 Q2 | A | 복제 시 상품·플랫폼·계정·템플릿은 유지하고 주문번호·입금·완료·AI 리뷰는 초기화한다. |
| 2차 Q3 | B | 초안과 사용자 기본값을 PC·모바일에서 공유할 수 있도록 DB 동기화를 설계한다. 민감 템플릿 원문은 초안 저장 대상에서 제외한다. |
| 2차 Q4 | C | 이름 붙인 저장 보기를 DB에 저장하고 여러 기기에서 동기화한다. URL 필터도 함께 유지한다. |
| 2차 Q5 | A | 배송·플랫폼·결제수단·구매계정·템플릿은 안전한 일괄 수정으로 제공하고, 완료처리는 별도 단계형 화면으로 분리한다. |
| 2차 Q6 | A | `orders.deleted_at` 기반 휴지통과 삭제 직후 실행 취소를 구현 범위에 포함한다. |
| 2차 Q7 | A | 자동추천 처리 후 다음 항목 자동 이동과 최근 처리/숨김 복구 화면을 포함한다. |
| 2차 Q8 | A | 기존 `scheduled_purchase_at`을 폼·장부·인앱 일정에 연결하고 리뷰 마감일·푸시는 우선 제외한다. |
| 2차 Q9 | 구현 안 함 | 엑셀 가져오기는 2차 구현 범위에서 제외하고 기존 내보내기만 유지한다. |
| 2차 Q10 | A | 템플릿 복제·기본 지정·삭제·사용 주문 수를 추가한다. |
| 2차 Q11 | C | PWA와 브라우저 푸시는 2차 구현 범위에서 제외하고 현재 웹 브라우저 서비스 형태를 유지한다. |
| 2차 Q12 | C | 입금계좌 설정 화면은 추가하지 않고 자동추천의 현재 표시만 유지한다. |
| 2차 Q13 | A | 2A를 먼저 완료·검증한 뒤 2B를 별도 작업으로 진행한다. |

### 구현 시 주의사항

- PWA와 브라우저 푸시는 구현하지 않는다. 기존 구매 예정 시각을 활용한 인앱 일정만 제공한다.
- Q3·Q4·Q6 선택으로 2차에는 DB 변경이 포함된다. 구현 시 사용자 설정/저장 보기/휴지통 스키마를 확정하고 `docs/guide_db.md`와 DB 타입을 함께 갱신한다.

## 11. 2차 레이아웃·가독성 개선 추천안

> 이 섹션은 선택 전 추천안이다. 사용자 답변과 다른 내용은 아래 `14. 레이아웃 답변 반영 및 확정안`을 최종 구현 기준으로 삼는다.

### 11-1. 공통 정보 위계

현재 각 화면은 폭은 충분히 넓어졌지만 카드, 표, 보조 패널이 페이지마다 다른 순서와 밀도로 배치되어 있다. 2차에서는 기능 추가와 함께 아래 다섯 영역의 순서를 통일하는 것을 권장한다.

```text
페이지 헤더
  └─ 제목 · 짧은 설명 · 계정/페이지 주요 액션
상태 스트립
  └─ 클릭 가능한 핵심 수치와 빠른 필터
업무 도구막대
  └─ 검색 · 저장 보기 · 필터 · 정렬 · 선택 모드
주 업무 영역
  └─ 표/목록/폼/차트
문맥 보조 영역
  └─ 오늘 할 일 · 현재 주문 요약 · 도움말 · 다음 행동
```

- 한 화면의 파란색 채움 버튼은 원칙적으로 가장 중요한 동작 1개만 둔다.
- 단순 설명을 위한 카드보다 실제 상태·액션이 있는 패널을 우선한다.
- 카드 안에 또 테두리 카드가 반복되는 구조를 줄이고, 하위 정보는 구분선과 옅은 배경으로 묶는다.
- 경고·미완료·오류만 색을 사용하고 일반 데이터 카드는 중립적인 흰색/회색을 사용한다.
- 페이지 제목은 `24px`, 섹션 제목은 `16~18px`, 표/폼 본문은 `14px`를 기본으로 맞춘다.
- 숫자는 우측 정렬과 `tabular-nums`를 유지하고, 금액·건수·비율의 단위를 레이블 또는 열 제목에서 한 번만 명확히 표시한다.

### 11-2. 공통 페이지 헤더와 사이드바

#### 페이지 헤더 추천

- 왼쪽: 상위 위치/페이지명, 제목, 현재 데이터 범위 또는 짧은 설명
- 오른쪽: 주요 액션 1개, 보조 액션 최대 2개, 나머지는 `더보기`
- 목록 화면은 헤더 전체를 고정하지 않고 검색·필터 도구막대만 스크롤 시 sticky로 유지한다.
- 모바일에서는 설명을 한 줄로 줄이고 주요 액션은 아이콘 또는 하단 sticky 버튼으로 이동한다.

#### 사이드바 추천

- `1280px` 이상: 현재처럼 240px 전체 사이드바
- `1024~1279px`: 72px 아이콘 레일로 축소해 주 업무 폭 확보
- 아이콘 레일은 hover에만 의존하지 않고 `title`, tooltip, 현재 메뉴 표시를 제공
- 사용자가 임의로 접고 펴는 상태보다 화면 폭에 따른 자동 전환을 기본으로 해 설정을 늘리지 않는다.

### 11-3. `/` 구매장부

현재 모바일에서는 주문 목록 뒤에 `오늘 확인할 일`이 배치돼 긴 목록을 지나야 볼 수 있고, 데스크톱에서는 미완료·완료 필터가 각각 나뉘어 있다. 2차 기능과 함께 아래 구조를 권장한다.

#### 데스크톱

```text
구매장부 헤더                         [전체 검색] [주문 추가]
전체 · 미완료 · 완료 · 미회수 원금 상태 스트립
저장 보기 | 빠른 필터 | 기간 | 검색 | 정렬 | 선택
┌──────────────────────────────┬────────────────────┐
│ 미완료/완료 탭형 주문 표       │ 오늘 확인할 일       │
│ sticky 표 헤더 · 가상 스크롤   │ 선택 조건 요약        │
│                              │ 빠른 작업             │
└──────────────────────────────┴────────────────────┘
```

- 미완료·완료는 같은 위치의 세그먼트 탭으로 전환하고 활성 목록만 렌더링한다.
- 완료 탭은 처음 열 때 현재 lazy loading을 수행한다.
- 검색·날짜·저장 보기·선택 모드를 하나의 도구막대로 통합한다.
- 필터 적용 시 상단에 제거 가능한 조건 칩을 보여주고 숨은 조건이 없게 한다.
- 표 헤더는 목록 스크롤 영역 상단에 고정한다.
- 우측 패널은 현재 선택한 저장 보기와 필터에 맞는 건수로 바뀌어야 한다.
- 표 밀도는 `편안하게/촘촘하게` 두 단계만 제공하고 열의 순서는 기본값을 유지한다.

#### 모바일

```text
구매장부                    [검색] [+]
오늘 할 일 4개 요약(2×2, 접기 가능)
[미완료 N] [완료 N]
빠른 필터 가로 스크롤
검색/정렬/선택 도구막대
주문 카드 목록
```

- `오늘 확인할 일`은 목록 위에 2×2 요약으로 축약하고 세부 금액은 펼쳤을 때 보여준다.
- 빠른 작업 카드 전체를 별도로 두지 않고 헤더의 주문 추가와 도구막대에 합친다.
- 선택 모드에서는 하단 메뉴 위에 선택 작업 막대를 고정하고 기존 스와이프 삭제는 잠시 비활성화한다.
- 카드 첫 줄은 상품명, 둘째 줄은 카톡방/구매일, 오른쪽은 구매금액 순으로 통일한다.
- 플랫폼·계정·배송 같은 보조 정보는 최대 두 줄로 제한하고 나머지는 펼침 영역에 둔다.

### 11-4. `/dashboard`

현재 기간 카드, KPI 5개, 차트 4개, 인사이트, 월별 표, 분류 표 3개가 모두 세로로 이어져 스크롤이 길다. 상단은 요약, 하단은 탭형 상세로 정리하는 것을 권장한다.

```text
대시보드 헤더                         [현재 범위 내보내기]
기간 프리셋 · 시작일 · 종료일 · 비교 기준
구매금액 | 입금금액 | 수익 | 미회수 | 완료율
┌──────────────────────────┬──────────────────┐
│ 월별 금액 흐름             │ 이번 기간 확인할 것 │
├──────────────────────────┼──────────────────┤
│ 완료율/배송률              │ 빠른 장부 이동       │
└──────────────────────────┴──────────────────┘
[월별] [플랫폼] [결제수단] [구매계정] 상세 탭
선택한 상세 표 1개
```

- 기간 선택은 페이지 상단의 한 줄 도구막대로 올리고 스크롤 시 sticky 처리한다.
- 누적 전체 KPI와 선택 기간 KPI가 혼동되지 않도록 기본은 `선택 기간`으로 통일하고 `전체 누적`은 별도 작은 요약으로 둔다.
- 첫 화면에는 핵심 차트 2개만 보여주고 플랫폼/구매계정 순위 차트는 상세 탭으로 이동한다.
- 월별·플랫폼·결제수단·구매계정 표는 동시에 세 개를 펼치지 않고 탭 하나만 표시한다.
- 모든 카드·차트·표 행은 구매장부의 실제 필터 결과로 이어진다.
- 모바일에서는 KPI를 가로 스크롤 카드 대신 2열 그리드로 두고, 차트 높이는 220~260px로 제한한다.

### 11-5. `/dashboard/monthly`

현재 주요/보조 지표 12개가 동일한 카드 형태라 무엇을 먼저 봐야 하는지 약하다.

- 첫 줄은 `구매금액`, `입금금액`, `수익`, `미회수 원금` 4개만 큰 KPI로 유지한다.
- 건수·완료율·배송률·평균수익은 `처리 현황`, `배송 현황`, `수익성` 세 그룹의 작은 설명 목록으로 묶는다.
- 전월 대비는 각 KPI 아래 한 줄 배지로 유지하고 색은 상승/하락 자체가 좋은지 나쁜지에 따라 의미를 구분한다.
- 데스크톱은 `월 상세 8열 + 이번 달 확인 항목 4열`, 모바일은 확인 항목을 KPI 바로 아래에 둔다.
- 플랫폼/결제수단/구매계정 표는 세 개 동시 배치 대신 탭 하나로 전환한다.
- 월 이동은 헤더 중앙에 두고 `이번 달로` 버튼을 추가한다.

### 11-6. `/menu-4` 자동추천

선택한 연속 처리 기능을 살리려면 데스크톱은 목록과 검수 문맥을 동시에 보는 master-detail이 가장 효율적이다.

#### 데스크톱 추천

```text
자동추천 헤더            진행 N / 전체 N       [크롤링 새로고침]
[주문 추천] [입금 추천] [최근 처리/숨김]
필터 · 정렬 · 처리 후 다음 항목 토글
┌────────────────────┬────────────────────────────┐
│ 대기열 목록 4~5열    │ 선택 항목 검수/추천 7~8열    │
│ 현재 행 강조         │ 주요 액션은 우측 상단/하단 고정 │
└────────────────────┴────────────────────────────┘
```

- 주문 추천은 왼쪽에 대기열, 오른쪽에 선택 주문 검수 폼을 둔다.
- 입금 추천은 왼쪽에 입금 목록, 오른쪽에 추천 후보를 둬 펼침 행이 길어지는 문제를 줄인다.
- 선택 항목이 없을 때 오른쪽은 다음 사용 방법과 강한 추천 기준을 보여준다.
- 익스텐션 안내는 항상 큰 카드로 노출하지 않고 헤더의 `연동 상태/설치 안내` 버튼에서 보조 패널로 연다.
- 모바일은 현재처럼 목록에서 상세로 이동하고, 상단에 진행률과 이전/다음 버튼을 둔다.
- 위험 동작은 색상 버튼만으로 구분하지 않고 텍스트 레이블과 확인 단계를 유지한다.

### 11-7. `/orders/new`, `/orders/detail`

현재 우측의 정적 체크리스트는 처음 몇 번 이후 효용이 낮고, 실제 저장 버튼은 긴 폼 아래에 있어 데스크톱의 우측 공간과 분리돼 있다.

#### 추천 구조

```text
주문 추가/상세 헤더       초안 저장 상태       [복제] [목록]
┌────────────────────────────┬────────────────────┐
│ 구매 정보                   │ 실시간 주문 요약      │
│ ─ 필수 정보                 │ 누락 필수 항목        │
│ ─ 분류/계정                 │ 중복 후보            │
│ ─ 리뷰 조건                 │ 저장 후 동작          │
│ ─ 추가 정보(접기)           │ [주요 저장/완료 버튼] │
│ 완료 정보 / AI 리뷰          │                     │
└────────────────────────────┴────────────────────┘
```

- 정적 등록 체크리스트를 `실시간 요약 + 누락 항목 + 중복 후보 + 저장 버튼`으로 교체한다.
- 폼 내부는 `필수 구매 정보`, `분류와 구매계정`, `리뷰 조건`, `추가 정보`, `입금/완료`, `AI 리뷰` 순서로 그룹화한다.
- 데스크톱에서 일반 입력 두 개는 한 줄에 두되, 카톡방 이름·상품명·메모·주소처럼 긴 값은 전체 폭을 사용한다.
- 수정 화면은 상단의 장식성 요약 hero와 우측 주문 요약이 정보를 중복하지 않도록 하나만 남긴다.
- 주요 저장/완료 버튼은 우측 sticky 패널에 두고, 모바일에서는 하단 메뉴 바로 위 sticky 버튼으로 전환한다.
- 삭제·미완료 전환 같은 보조/위험 액션은 `더보기`에 묶고 주요 저장 버튼과 거리를 둔다.
- AI 리뷰 결과는 긴 텍스트이므로 기본 구매 입력과 같은 카드 안에 섞지 않고 별도 섹션 또는 접힘 패널로 둔다.

### 11-8. `/settings`

- 설정 홈은 세로 메뉴 행 대신 `계정`, `주문 기본값`, `템플릿`, `AI 리뷰`, `마스터 데이터`, `휴지통` 카드 허브를 2~3열로 배치한다.
- 각 카드에는 설명 한 줄과 현재 상태만 표시한다. 예: 템플릿 4개, 저장 보기 3개, 휴지통 2건.
- 하위 설정은 1440px 전체를 채우지 않고 읽기 쉬운 `720~960px` 본문 폭을 사용한다.
- 플랫폼·결제수단·구매계정처럼 목록 관리 화면은 데스크톱에서 `목록 2/3 + 새 항목 추가/설명 1/3`로 배치한다.
- 휴지통은 주문명·삭제일·복구·영구삭제를 표로 보여주고 영구삭제는 가장 약한 빈도로 노출한다.
- 모바일은 카드 허브 1열과 현재 드릴다운을 유지한다.

### 11-9. 구매 정보 템플릿 추가/수정

- 왼쪽 입력을 `템플릿 이름`, `구매자/수취인`, `배송/연락처`, `계정/정산` 네 그룹으로 나눈다.
- 각 그룹은 별도 큰 카드보다 한 카드 안의 제목·구분선을 사용해 테두리 중첩을 줄인다.
- 데스크톱 우측 미리보기는 sticky로 유지하고 `입력된 필드 수`, `기본 템플릿 지정`, `복사`, `저장`을 함께 둔다.
- 민감한 계좌번호·연락처는 미리보기에서 기본 마스킹하고 `원문 보기`를 눌렀을 때만 현재 세션에서 표시한다.
- 모바일은 폼 다음에 미리보기를 두되 저장 버튼은 하단 sticky로 유지한다.
- 복제 진입 시 상단에 `OO 템플릿을 복제 중`을 명확히 표시해 원본 수정과 혼동하지 않게 한다.

### 11-10. `/login`과 공통 상태 화면

- 현재 소개+로그인 2열 구조는 유지한다.
- 소개 영역의 기능 카드는 3개 이하로 제한하고 로그인 카드보다 시각적으로 강하지 않게 한다.
- 로딩·빈 상태·오류 상태는 페이지 레이아웃 안에서 같은 폭과 헤더 위치를 사용한다.
- 오류는 메시지만 보여주지 않고 `다시 시도`, `목록으로`, `로그인으로` 중 가능한 복구 동작을 함께 둔다.

## 12. 레이아웃 구현 원칙과 검증

- 공통 헤더·도구막대·상태 스트립은 기존 페이지의 반복 구조를 조합할 수 있을 때 우선 재사용한다.
- 모든 페이지에 하나의 거대한 공통 컴포넌트를 만들지 않고, 실제로 두 화면 이상에서 동일한 구조만 공통화한다.
- 모바일과 데스크톱에서 정보 의미와 기능은 같게 유지하고 표시 순서·밀도만 조정한다.
- sticky 영역은 데스크톱 사이드바, 모바일 하단 메뉴, 폼 저장 버튼과 서로 겹치지 않게 실제 높이를 기준으로 계산한다.
- 테이블 전체 페이지 가로 스크롤은 만들지 않고 테이블 컨테이너 안에서만 스크롤한다.
- 200% 브라우저 확대, 키보드 탭 이동, 긴 한국어 상품명, 큰 금액, 빈 데이터 상태를 함께 점검한다.
- 사용자가 선택한 기능 범위의 테스트와 함께 390px, 768px, 1024px, 1280px, 1440px에서 시각 점검한다.

## 13. 레이아웃 결정 질문과 사용자 답변

기존 기능 질문과 혼동하지 않도록 아래 질문은 `UI Q` 번호를 사용한다.

### UI Q1. 전체 화면의 기본 정보 밀도는 어떻게 할까요?

- **A (추천)**: 균형형 — 핵심 수치와 주 업무는 바로 보이고 상세 정보는 탭·접힘으로 정리
- B: 고밀도형 — 여백과 카드 높이를 줄여 한 화면에 최대한 많은 행과 수치를 표시
- C: 여유형 — 카드와 설명을 크게 유지하고 스크롤을 허용

### UI Q2. 1024~1279px 노트북에서 사이드바는 어떻게 할까요?

- **A (추천)**: 자동으로 72px 아이콘 레일로 축소하고 1280px 이상에서 240px 전체 표시
- B: 현재처럼 1024px 이상에서 항상 240px 고정
- C: 사용자가 직접 접고 펴며 마지막 상태를 기억

### UI Q3. 공통 페이지 헤더를 어느 정도 고정할까요?

- **A (추천)**: 제목 헤더는 일반 스크롤, 검색·필터·선택 도구막대만 sticky
- B: 제목과 모든 액션을 포함한 전체 헤더를 sticky
- C: sticky 없이 현재처럼 페이지마다 배치

### UI Q4. 구매장부의 미완료·완료 목록은 어떻게 배치할까요?

- **A (추천)**: 같은 영역의 탭으로 전환하고 활성 목록만 표시
- B: 현재처럼 미완료 위, 완료 아래의 두 섹션 유지
- C: 데스크톱에서 미완료·완료를 좌우 2열로 동시 표시

### UI Q5. 모바일 구매장부에서 `오늘 확인할 일`은 어디에 둘까요?

- **A (추천)**: 목록 위 2×2 축약 요약으로 표시하고 세부 내용은 접기
- B: 현재처럼 주문 목록 뒤에 전체 패널 표시
- C: 별도의 `오늘 할 일` 화면으로 분리

### UI Q6. 구매장부 PC 표의 도구와 밀도는 어떻게 할까요?

- **A (추천)**: 검색·필터·저장 보기·선택을 한 줄 도구막대로 통합하고 `편안하게/촘촘하게` 밀도 전환 제공
- B: 현재처럼 미완료·완료 섹션마다 필터 버튼 제공
- C: 표 대신 PC에서도 카드 목록 사용

### UI Q7. 대시보드 하단의 차트·표는 어떻게 정리할까요?

- **A (추천)**: 핵심 차트 2개만 먼저 표시하고 월별/플랫폼/결제수단/계정 상세는 탭으로 전환
- B: 현재처럼 모든 차트와 표를 세로로 계속 표시
- C: 사용자가 대시보드 블록을 직접 숨기고 순서를 바꾸게 함

### UI Q8. 월별 상세의 12개 지표는 어떻게 보여줄까요?

- **A (추천)**: 핵심 금액 4개만 큰 카드, 나머지는 처리/배송/수익성 그룹의 작은 목록
- B: 현재처럼 모든 지표를 같은 크기의 카드로 표시
- C: 카드보다 상세 표를 먼저 표시

### UI Q9. 자동추천 PC 검수 화면은 어떤 구조가 좋을까요?

- **A (추천)**: 왼쪽 대기열 + 오른쪽 선택 항목 검수의 master-detail
- B: 현재처럼 목록에서 별도 전체 폭 상세 화면으로 이동
- C: 목록 위에 오른쪽 슬라이드 패널을 열어 검수

### UI Q10. 주문 추가·상세의 우측 패널은 무엇을 보여줄까요?

- **A (추천)**: 실시간 주문 요약·누락 항목·중복 후보·저장/완료 액션
- B: 입력 섹션 목차와 도움말만 표시하고 저장 버튼은 폼 마지막 유지
- C: 우측 패널 없이 폼을 전체 폭으로 표시

### UI Q11. 주문 폼의 긴 섹션은 어떻게 구분할까요?

- **A (추천)**: 한 페이지 흐름을 유지하고 추가 정보·AI 리뷰만 접을 수 있게 구성
- B: 구매정보/완료정보/AI 리뷰를 상단 탭으로 분리
- C: 단계별 다음 버튼이 있는 마법사 형태

### UI Q12. 설정 홈과 하위 화면은 어떻게 배치할까요?

- **A (추천)**: 설정 홈은 2~3열 카드 허브, 하위 화면은 720~960px 본문과 필요한 경우 목록/추가 2열
- B: 현재처럼 전체 폭 세로 메뉴와 한 열 하위 화면
- C: 글로벌 사이드바 안에 모든 설정 입력을 직접 펼침

### UI Q13. 템플릿 입력 화면은 어떻게 그룹화할까요?

- **A (추천)**: 한 카드 안에서 이름/구매자/배송/정산 섹션으로 구분하고 우측 sticky 미리보기 유지
- B: 현재처럼 필드를 한 줄 흐름으로 나열
- C: 섹션별 단계형 마법사로 전환

### UI Q14. 모바일의 저장·완료 같은 주요 액션은 어떻게 둘까요?

- **A (추천)**: 가장 중요한 버튼 1개만 하단 sticky, 나머지는 `더보기` 또는 본문에 배치
- B: 가능한 액션을 하단에 여러 버튼으로 항상 표시
- C: sticky 없이 각 폼 마지막에만 표시

### UI Q15. 상태 색상 사용량은 어떻게 할까요?

- **A (추천)**: 기본 화면은 중립색으로 두고 미완료·주의·오류·성공처럼 의미가 있을 때만 색 사용
- B: 현재처럼 카드와 상태별 배경색을 적극적으로 사용
- C: 사용자가 상태 색을 설정에서 직접 선택

### UI Q16. 반응형 단계를 어떻게 나눌까요?

- **A (추천)**: 모바일 `<768`, 태블릿 `768~1023`, 축소 데스크톱 `1024~1279`, 전체 데스크톱 `1280+`의 4단계
- B: 현재처럼 모바일과 데스크톱 중심의 단순한 단계 유지
- C: 화면별로 필요한 breakpoint를 각각 다르게 사용

답변 예시: `UI Q1 A, Q2 A, Q3 A ... UI Q16 A`

UI Q1: B
UI Q2: B
UI Q3: A
UI Q4: B
UI Q5: A
UI Q6: A
UI Q7: B
UI Q8: A
UI Q9: A
UI Q10: A
UI Q11: A
UI Q12: A
UI Q13: A
UI Q14: A
UI Q15: A
UI Q16: A

## 14. 레이아웃 답변 반영 및 확정안

사용자가 남긴 원문 답변은 위에 그대로 보존한다. 추천안과 선택이 다른 경우 이 섹션의 확정안을 우선하며, 아직 답이 없는 항목은 임의로 구현 범위를 정하지 않는다.

| 질문 | 선택 | 최종 반영 |
| --- | --- | --- |
| UI Q1 | B | 기본 화면을 고밀도로 구성해 같은 높이에서 더 많은 주문 행과 지표를 보여준다. |
| UI Q2 | B | `1024px` 이상에서는 240px 전체 사이드바를 유지한다. 아이콘 레일과 사용자 접기 기능은 추가하지 않는다. |
| UI Q3 | A | 페이지 제목은 일반 스크롤로 두고 검색·필터·선택 도구막대만 필요한 화면에서 sticky로 유지한다. |
| UI Q4 | B | 구매장부는 미완료 목록 위, 완료 목록 아래의 두 섹션을 유지한다. 완료 목록의 기본 접힘과 지연 조회도 유지한다. |
| UI Q5 | A | 모바일 구매장부의 `오늘 확인할 일`을 목록 위 2×2 축약 요약으로 이동하고 상세 내용은 접을 수 있게 한다. |
| UI Q6 | A | 구매장부 검색·필터·저장 보기·선택 기능을 공통 도구막대로 합치고 표 밀도 전환을 제공한다. 기본값은 Q1에 맞춰 `촘촘하게`로 둔다. |
| UI Q7 | B | 대시보드의 기존 차트와 표를 모두 한 페이지에 유지하고 상세 탭으로 숨기지 않는다. |
| UI Q8 | A | 월별 상세는 핵심 금액 4개만 큰 KPI로 두고 나머지는 처리·배송·수익성 그룹의 작은 목록으로 정리한다. |
| UI Q9 | A | 자동추천 PC 화면을 왼쪽 대기열과 오른쪽 검수 영역의 master-detail 구조로 바꾼다. |
| UI Q10 | A | 주문 추가·상세 우측 패널에 실시간 요약, 누락 필수값, 중복 후보, 저장·완료 액션을 표시한다. |
| UI Q11 | A | 주문 폼은 한 페이지 흐름을 유지하고 추가 정보와 AI 리뷰 섹션만 접을 수 있게 구성한다. |
| UI Q12 | A | 설정 홈을 2~3열 카드 허브로 만들고 하위 화면은 720~960px 본문 또는 목록·추가 2열로 구성한다. |
| UI Q13 | A | 템플릿 폼을 한 카드 안의 네 섹션으로 묶고 데스크톱 우측 sticky 미리보기를 유지한다. |
| UI Q14 | A | 모바일에서는 가장 중요한 액션 하나만 하단에 고정하고 나머지는 `더보기` 또는 본문에 둔다. |
| UI Q15 | A | 일반 화면은 중립색으로 정리하고 미완료·주의·오류·성공처럼 상태 의미가 있을 때만 색을 사용한다. |
| UI Q16 | A | 모바일, 태블릿, 축소 데스크톱, 전체 데스크톱의 네 반응형 구간을 사용한다. |

### 14-1. 공통 레이아웃 확정 기준

- 정보 밀도는 `촘촘하게`를 기본값으로 한다. 카드 내부 여백과 행 높이를 줄이되 버튼의 터치 영역, 본문 글자 크기, 키보드 포커스 영역은 축소하지 않는다.
- `1024px` 이상에서는 240px 사이드바를 유지한다. 특히 `1024~1279px`에서는 남은 본문 폭이 좁으므로 보조 설명을 줄이고 표·폼을 우선 배치한다.
- 페이지 제목과 설명은 스크롤되고, 검색·필터·선택처럼 긴 목록을 조작하는 도구막대만 sticky 처리한다.
- 파란색 채움 버튼은 화면의 주요 액션 하나에 사용하고 일반 카드와 표는 흰색·회색 중심으로 정리한다.
- 고밀도는 정보 생략이 아니라 간격 축소를 뜻한다. 모바일과 데스크톱에서 같은 상태와 주요 액션을 제공한다.

### 14-2. 페이지별 최종 배치

#### `/` 구매장부

```text
구매장부 헤더                              [전체 검색] [주문 추가]
전체 · 미완료 · 완료 · 미회수 원금 상태 스트립
저장 보기 | 빠른 필터 | 기간 | 검색 | 정렬 | 선택 | 밀도
┌────────────────────────────────────┬──────────────────┐
│ 미완료 주문 표                       │ 오늘 확인할 일     │
├────────────────────────────────────┤ 현재 조건 요약     │
│ 완료 주문 표(기본 접힘·열 때 조회)    │ 빠른 작업          │
└────────────────────────────────────┴──────────────────┘
```

- 미완료·완료를 탭으로 바꾸지 않고 기존 순서를 유지하며 두 목록에 같은 검색·필터 조건을 적용한다.
- 완료 주문은 기본 접힘과 최초 펼침 시 조회하는 현재 성능 최적화를 유지한다.
- 도구막대는 sticky로 두고, 기본 표 밀도는 `촘촘하게`, 선택 가능한 보조 밀도는 `편안하게`로 둔다.
- `1024~1279px`에서는 고정 사이드바로 본문이 좁아지므로 우측 `오늘 확인할 일` 패널을 표 위의 한 줄 상태 스트립으로 축약한다. `1280px` 이상에서만 표 우측 패널을 사용한다.
- 모바일에서는 `오늘 확인할 일` 2×2 요약을 주문 목록보다 먼저 보여주고, 미완료 다음 완료 섹션 순서를 유지한다.

#### `/dashboard`

- 기간 도구막대와 핵심 KPI를 먼저 배치하되, 기존 차트와 상세 표는 탭으로 숨기지 않고 모두 표시한다.
- 긴 스크롤에서도 현재 위치를 알 수 있도록 `금액 흐름`, `처리율`, `분류별 현황`, `상세 표`의 제목과 간격을 통일한다.
- `1280px` 이상에서는 관련 차트 두 개를 한 행에 배치할 수 있지만 모든 차트·표는 같은 페이지에 계속 노출한다.
- `1024~1279px`와 모바일에서는 차트와 표를 한 열로 배치해 240px 사이드바와 차트 최소 폭이 충돌하지 않게 한다.
- 기간 선택 도구막대만 sticky로 유지하고 KPI와 차트 제목은 일반 스크롤로 둔다.

#### `/dashboard/monthly`

- 구매금액·입금금액·수익·미회수 원금만 큰 KPI 카드로 표시한다.
- 나머지 수치는 `처리 현황`, `배송 현황`, `수익성` 그룹 안의 고밀도 설명 목록으로 배치한다.
- 플랫폼·결제수단·구매계정 상세는 Q7의 전체 노출 원칙을 적용해 탭으로 숨기지 않는다. 명확한 섹션 제목과 표 간 간격으로 구분한다.
- 모바일에서는 4개 KPI를 2×2로 배치하고 보조 지표 목록을 바로 이어서 표시한다.

#### `/menu-4` 자동추천

- 데스크톱은 왼쪽 대기열, 오른쪽 검수·추천의 master-detail로 구성하고 처리 후 다음 항목을 같은 자리에서 연다.
- `1024~1279px`에서도 240px 사이드바를 유지하므로 대기열은 약 240~280px로 제한하고 검수 폼은 한 열 입력을 기본으로 한다.
- `1280px` 이상에서는 대기열과 상세 영역의 비율을 약 4:8로 넓힌다.
- 모바일과 태블릿은 목록과 상세를 한 화면에 억지로 나누지 않고 목록에서 상세로 이동하는 흐름과 이전·다음 동작을 제공한다.

#### `/orders/new`, `/orders/detail`

- 데스크톱 우측 sticky 패널에 실시간 주문 요약, 누락 항목, 중복 후보와 주요 저장·완료 버튼을 모은다.
- `1024~1279px`에서는 우측 패널 폭을 약 260~280px로 제한하고 폼의 긴 입력은 한 열로 배치한다.
- 모바일에서는 요약을 폼 위의 접힘 영역으로 이동하고 주요 액션 하나만 하단 메뉴 위에 고정한다.
- 주문 폼은 탭이나 단계형 화면으로 나누지 않고 한 페이지 입력 흐름을 유지한다.
- `필수 구매 정보`, `분류와 구매계정`, `리뷰 조건`은 기본으로 펼치고 `추가 정보`, `AI 리뷰`만 접을 수 있게 구성한다.

#### `/settings`, 구매 정보 템플릿

- 설정 홈은 데스크톱 2~3열, 태블릿 2열, 모바일 1열 카드 허브로 구성한다.
- 설정 하위 화면은 불필요하게 전체 너비를 채우지 않고 내용에 따라 720~960px 폭을 사용한다.
- 템플릿 입력은 `템플릿 이름`, `구매자/수취인`, `배송/연락처`, `계정/정산` 순서로 그룹화한다.
- 템플릿 미리보기는 데스크톱에서 sticky 우측 패널, 모바일에서 폼 아래 접힘 영역으로 표시하며 주요 저장 버튼 하나만 하단에 고정한다.

### 14-3. 반응형 구간별 기준

| 화면 폭 | 내비게이션 | 기본 배치 |
| --- | --- | --- |
| `<768px` | 모바일 하단 메뉴 | 1열, 오늘 할 일 우선, 주요 액션 1개 하단 고정 |
| `768~1023px` | 모바일/태블릿 내비게이션 유지 | 1~2열, 표보다 카드 또는 내부 가로 스크롤, master-detail은 화면 전환 |
| `1024~1279px` | 240px 전체 사이드바 | 고밀도 본문, 차트 1열, 보조 패널 축소, 자동추천만 좁은 master-detail |
| `1280px+` | 240px 전체 사이드바 | 표+보조 패널, 차트 2열, 넓은 master-detail 허용 |

### 14-4. 최종 확정 상태

- 2차 기능 질문 Q1~Q13과 레이아웃 질문 UI Q1~Q16의 답변이 모두 반영됐다.
- PWA, 브라우저 푸시, 엑셀 가져오기, 별도 입금계좌 설정 화면은 이번 구현 범위에서 제외한다.
- 레이아웃은 고밀도 화면, 240px 고정 사이드바, 미완료·완료 세로 배치, 전체 대시보드 항목 유지, 자동추천 master-detail, 한 페이지 주문 폼을 최종 기준으로 삼는다.
- 추가로 확인할 선택 질문은 없으며, 2A 구현과 검증을 마친 뒤 2B를 진행한다.

## 15. 2A 구현 결과

2026-08-08에 2A 범위를 구현하고 검증했다.

- 주문 복제, 저장 후 이동 방식, 서버 동기화 초안, 사용자 기본값·최근값, 저장 전 중복 후보 확인을 추가했다.
- 구매장부에 통합 도구막대, 빠른 필터, URL 필터, DB 저장 보기, 표시 밀도 전환, 구매 예정 일정 표시를 연결했다.
- 메뉴·설정·주문·템플릿을 찾는 전체 검색과 `Ctrl/Cmd + K` 단축키를 추가했다.
- 자동추천을 데스크톱 master-detail 구조로 바꾸고 처리 후 다음 항목 이동 및 키보드 이동을 추가했다.
- 대시보드 지표·분류 항목의 장부 드릴다운과 현재 기간 엑셀 내보내기를 추가했다.
- 템플릿 복제·기본 지정·삭제·사용 주문 수를 추가하고 설정 홈과 주문/월별 상세의 정보 위계를 확정안에 맞게 정리했다.
- `user_preferences`, `user_order_drafts`, `saved_order_views` 마이그레이션을 적용하고 RLS 및 역할 권한을 제한했다. 변경된 전체 스키마와 `docs/guide_db.md`를 동기화했다.
- 날짜·구매 일정·중복 정규화 단위 테스트 5개, ESLint, TypeScript, 프로덕션 빌드, 390px·1024px·2560px 브라우저 점검을 통과했다.

2B의 주문 선택·일괄 수정·일괄 완료·숨김 복구·휴지통은 이번 구현에 포함하지 않았으며 다음 작업으로 남긴다.
