# 웹 전용 화면 정보 밀도·PC 레이아웃 개선 계획

작성일: 2026-08-08<br>
상태: 1차 구현 완료 / 검증 완료 (2026-08-08)

## 1. 목표

모바일 브라우저를 고려해 좁게 만들어진 현재 화면을 웹 전용 서비스에 맞게 확장한다. PC에서는 양옆의 빈 공간을 업무에 필요한 요약·주의사항·빠른 동작·상세 정보에 사용하고, 모바일에서는 현재의 카드형 목록·하단 메뉴·스와이프 흐름을 유지한다.

이번 계획의 기본 방향은 다음과 같다.

- 이미 저장된 주문·입금·플랫폼·결제수단·구매계정·리뷰 정보를 우선 활용한다.
- 단순히 카드와 컬럼을 늘리기보다 사용자가 “무엇을 먼저 처리해야 하는지” 판단할 수 있는 정보를 추가한다.
- 데스크톱은 넓은 콘텐츠 셸과 `main + aside` 구조를 사용하고, 모바일은 `main` 아래로 보조 영역을 쌓는다.
- 기존의 주문 완료처리, 가상 스크롤, 자동추천 후보 계산, 크롬 익스텐션 기능은 변경하지 않는다.
- 1차 작업에서는 DB 스키마를 변경하지 않는다. 새 작업 이력·알림·사용량 집계가 필요해지면 별도 결정 후 진행한다.

## 2. 현재 확인한 구조와 문제 지점

현재 라우트와 주요 책임은 아래와 같다.

| 화면 | 현재 구조 | PC에서 개선할 점 |
| --- | --- | --- |
| `/` 구매장부 | `HomePage` + `OrdersTable`, `max-w-4xl`, 통계 3개와 미완료/완료 주문 목록 | 목록 옆의 운영 요약·처리 우선순위 영역이 없고, 넓은 화면에서 목록만 중앙에 남음 |
| `/dashboard` 대시보드 | `OrdersDashboard`, `max-w-screen-xl`, 기간 조회·KPI·월별/분류별 표가 세로로 이어짐 | 넓이는 확보되어 있지만 정보가 세로로 길고, 기간 판단과 다음 행동이 분리됨 |
| `/dashboard/monthly` 월별 상세 | `max-w-screen-md`, 월별 KPI 12개와 분류별 표 | PC에서 가장 큰 좌우 여백이 생기며, 숫자 요약과 실제 확인할 주문이 떨어져 있음 |
| `/menu-4` 자동 추천 | `CrawlOrdersPage`, 2페이지 슬라이더, 주문 추천/입금 추천을 한 번에 하나만 마운트 | PC에서도 모바일과 같은 폭·슬라이더 중심이라 목록 옆의 설명·요약을 활용하지 못함 |
| `/orders/new` 주문 추가 | `OrderDetailForm`, `max-w-4xl`, 여러 입력 카드가 한 줄 흐름 | 입력 중 현재 값의 요약, 누락 항목, 여러 계정 생성 결과를 한눈에 보기 어려움 |
| `/orders/detail` 주문 상세 | `OrderDetailForm`, 상단에 목록 이동 버튼만 있음 | 주문 상태·금액·배송·리뷰 진행 상태를 폼 옆에서 계속 확인하기 어려움 |
| `/settings` 설정 | `SettingsPanel` 내부 뷰 전환, 단일 세로 메뉴 | PC에서도 모바일식 드릴다운을 사용해 빈 공간이 생기고 설정 항목 간 관계가 약함 |
| 템플릿 추가/수정 | `PurchaseInfoTemplateForm`, `max-w-4xl`, 입력 폼 단일 열 | 입력값이 실제 카톡 붙여넣기 결과로 어떻게 보이는지 즉시 확인하기 어려움 |
| `/login` 로그인 | 중앙 카드형 로그인 | 인증 화면의 여백은 의도된 부분이지만 PC에서 서비스 설명과 로그인 목적을 함께 보여줄 수 있음 |

공통으로 `BottomMenu`는 현재 `max-w-4xl`이고, 인증을 제외한 모든 화면에 고정되어 있다. 모바일에서는 유지하되 데스크톱에서는 별도 내비게이션을 둘지 결정이 필요하다.

## 3. 공통 레이아웃 계획

### 3-1. 데스크톱 셸

기본안은 `1024px` 이상에서 다음 구조를 적용한다.

```text
┌────────────────────────────────────────────────────────────┐
│ 데스크톱 내비게이션 │ 페이지 헤더: 제목 · 상태 · 계정 · 주요 액션 │
├────────────────────┼───────────────────────────────────────────┤
│                    │ main 콘텐츠                               │
│                    │ ┌──────────────────────┬────────────────┐ │
│                    │ │ 주 업무 영역          │ 보조 정보/액션 │ │
│                    │ │ 목록·폼·표             │ 요약·주의·링크 │ │
│                    │ └──────────────────────┴────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

- 콘텐츠 컨테이너는 현재 `max-w-4xl`에 고정하지 않고, 화면 폭에 따라 `minmax(0, 1fr)`로 늘어나도록 한다.
- 기본 목표 폭은 콘텐츠 기준 `1280~1440px`로 두고, 너무 넓은 모니터에서는 읽기 어려워지지 않도록 내부 표·폼별 최대 폭을 따로 둔다.
- 메인/보조 영역은 12열 기준으로 나누되, 보조 영역이 짧은 화면에서 목록을 지나치게 좁히지 않도록 `8:4` 또는 `9:3` 비율을 사용한다.
- 페이지 헤더는 제목, 현재 범위/상태, 새로고침·추가·돌아가기 같은 해당 화면의 주요 액션을 한 줄에 배치한다.
- 보조 패널은 긴 화면에서 `sticky`를 사용할 수 있지만, 모바일의 고정 하단 메뉴와 겹치지 않도록 한다.
- 장식용 빈 카드나 의미 없는 그래프는 추가하지 않는다. 각 영역에는 “판단할 수 있는 정보” 또는 “바로 실행할 수 있는 동작”만 둔다.

### 3-2. 모바일 셸

- 현재 하단 메뉴, `safe-area-inset-bottom`, 모바일 카드/테이블 전환, 터치 가능한 최소 버튼 크기를 유지한다.
- 데스크톱의 보조 패널은 본문 아래로 이동하고, 중요한 경고·완료 액션은 목록보다 위에 둔다.
- 모바일에서는 검색·필터·부가 설명을 접을 수 있게 하되, 처리 건수와 핵심 상태는 접지 않는다.
- PC 전용 hover에 의존하는 삭제·상태 표시를 만들지 않는다. 현재처럼 모바일에서는 액션을 항상 보이거나 명확한 버튼으로 제공한다.
- 가로 스크롤이 필요한 표는 표 전체를 감싸고, 페이지 전체가 가로로 밀리지 않도록 한다.

### 3-3. 공통 상태·접근성

- 로딩·빈 상태·오류 상태도 넓어진 레이아웃에 맞는 영역을 사용하고, 오류에는 해당 화면으로 돌아가거나 다시 시도하는 액션을 둔다.
- 색상은 `DESIGN.md`의 따뜻한 캔버스, 흰색 패널, hairline 경계, primary blue를 따른다. 상태 색은 완료·주의·오류의 의미 전달에만 사용한다.
- 테이블 행·카드·아이콘 버튼의 키보드 포커스와 `aria-label`을 유지한다.
- 새 요약 수치에는 로딩 중 값과 데이터 없음(`-` 또는 `0`)을 명확히 구분한다.

## 4. 페이지별 정보 추가·배치 계획

### 4-1. `/` 구매장부

대상 파일: `src/components/pages/home-page.tsx`, `src/components/orders/orders-table.tsx`

#### 추가할 정보

- 상단 요약 스트립에 전체 주문·미완료·완료·완료율을 표시한다. 가능하면 미완료 구매원금도 함께 보여준다.
- PC 보조 패널에 `오늘 확인할 일`을 둔다.
  - 입금 미완료 건수와 미완료 구매금액
  - 미배송 건수
  - 예약 구매 시각이 가까운 주문
  - 완료됐지만 입금 정보가 비어 있거나 확인이 필요한 주문
- `빠른 작업`에 주문 추가, 자동추천, 대시보드 이동을 둔다.
- 현재 주문 행에 이미 있는 상품명·구매일·구매금액·플랫폼·결제수단·구매계정·배송/입금 상태를 우선순위가 보이도록 재배치한다.
- PC에서 필터를 항상 펼쳐두고 검색어·구매일·상태를 한 줄에 배치한다. 모바일은 현재처럼 필터 버튼을 눌러 여는 방식을 유지한다.
- 완료 주문을 펼쳤을 때 최근 완료 요약과 전체 대비 완료율을 함께 보여준다.

#### 배치

- 데스크톱: 상단 요약은 전체 폭, 그 아래 `주문 목록 8~9열 + 오늘 확인/빠른 작업 3~4열`로 배치한다.
- 주문 목록은 현재 가상 스크롤과 데스크톱 표/모바일 카드를 유지한다. 표의 남는 폭에는 `상태`, `입금 차액 또는 수익`, `예약/배송 정보` 중 선택된 항목만 추가한다.
- 모바일: 요약 카드 → 오늘 확인할 일 → 미완료 주문 → 완료 주문 순서로 쌓고, 주문 추가 FAB는 하단 메뉴와 겹치지 않게 유지한다.

#### 주의점

- 완료 주문은 현재 지연 조회되므로 완료 금액 기반 요약을 처음부터 보여주려면 가벼운 집계 조회가 필요하다. 이를 위해 완료 전체 목록을 무리하게 초기 로드하지 않는다.
- 기존 완료처리 입력, 금액 보정, 확인창, 삭제, 가상 스크롤은 동작을 그대로 둔다.

### 4-2. `/dashboard` 대시보드

대상 파일: `src/components/pages/dashboard-page.tsx`, `src/components/orders/orders-dashboard.tsx`, `src/lib/dashboard-stats.ts`

#### 추가할 정보

- 현재 선택 기간과 직전 동일 기간을 비교하는 구매금액·입금금액·수익·완료율 변화량을 표시한다.
- KPI에 누적 구매금액·누적 입금금액·미회수 원금·미완료 건수 외에 완료율 또는 미배송 건수를 추가한다.
- 월별 요약 표에서 월을 누르면 `/dashboard/monthly?month=...`로 이동하도록 연결한다.
- `운영 인사이트` 영역에 다음 중 기존 데이터로 계산 가능한 항목을 표시한다.
  - 가장 구매금액이 큰 플랫폼/구매계정
  - 미완료 금액이 큰 기간
  - 미배송 건수와 완료율
  - 선택 기간의 평균 수익
- 그래프는 **Recharts**를 사용한다. 차트가 로드되지 않거나 보조기술을 사용하는 경우에도 기존 표와 수치가 항상 남도록 한다.

#### 대시보드에 권장하는 차트 기능

우선순위를 나눠 구현하되, 아래 기능은 모두 기존 주문 데이터와 기간 집계로 만들 수 있는 범위다.

- **월별 구매·입금·수익 추이**: 구매금액/입금금액은 막대, 수익은 선으로 겹쳐 보여 세 금액의 흐름을 한 번에 비교한다.
- **완료율·배송률 추이**: 월별 완료율과 배송률을 0~100% 축의 선 또는 영역 차트로 보여 운영 상태를 확인한다.
- **미회수 원금 추이**: 월별 미완료 주문의 구매원금을 누적 막대로 표시해 자금이 묶이는 시점을 찾는다.
- **플랫폼·결제수단·구매계정별 비교**: 구매금액/입금금액/수익 중 하나를 선택해 가로 막대 순위를 바꿔 본다.
- **기간 비교 오버레이**: 현재 기간과 직전 동일 기간을 색상·증감률·툴팁으로 비교한다.
- **KPI 미니 스파크라인**: 누적 구매금액·입금금액·수익·미완료 원금 카드 안에 최근 월 흐름을 작게 표시한다.
- **차트 범례 토글**: 구매금액·입금금액·수익처럼 여러 지표가 겹치는 차트는 범례를 눌러 필요한 선만 남긴다.
- **차트 툴팁**: 월, 분류명, 건수, 금액, 비율을 원화·퍼센트 형식으로 표시하고 `tabular-nums`를 유지한다.
- **월별 상세 이동**: 월별 차트의 점/막대를 선택하면 해당 월 상세 화면으로 이동한다. 기존 `/dashboard/monthly?month=...` 경로를 재사용한다.
- **분류 차트와 표 동기화**: 분류 차트의 범례·순위와 아래 표의 정렬 순서를 일치시켜 차트와 숫자를 따로 해석하지 않게 한다.
- **미완료 집중 표시**: 미완료 금액이나 확인 필요 비중이 전체에서 높아지는 월은 주의 색으로 표시하되, 장식적인 색상 사용은 피한다.
- **반응형 차트**: PC에서는 충분한 높이와 범례를 사용하고, 모바일에서는 차트 높이를 줄이며 필요한 경우 내부 가로 스크롤을 사용한다.
- **빈 데이터·단일 데이터 처리**: 데이터가 없으면 빈 차트 대신 설명을 표시하고, 월이 한 개뿐이면 추세를 과장하지 않고 단일 값으로 보여준다.
- **표 기반 접근성 대체**: 차트 아래에 동일한 수치의 요약 표 또는 화면낭독기용 설명을 제공한다.

차트의 1차 핵심은 월별 금액 추이, 완료율/배송률, 분류별 가로 막대, 월별 상세 이동으로 한다. 스파크라인·범례 토글·기간 오버레이는 구현 난이도와 화면 밀도를 확인한 뒤 같은 작업에서 포함한다.

#### 배치

- 데스크톱: 기간 필터/대상 건수는 전체 폭, KPI는 4~5열, 다음 줄은 `월별 추이 8열 + 운영 인사이트 4열`, 하단은 플랫폼·결제수단·구매계정 표 3열로 배치한다.
- 월별 표는 가로 스크롤을 유지하되, PC에서는 충분한 폭을 사용해 금액·건수 열을 읽기 쉽게 한다.
- 모바일: 기간 필터 → KPI 2열 → 인사이트 → 월별 표 → 분류별 표 순으로 세로 배치한다.

### 4-3. `/dashboard/monthly` 월별 상세

대상 파일: `src/components/pages/monthly-dashboard-detail-page.tsx`

#### 추가할 정보

- 현재 월과 전월의 구매금액·입금금액·수익·완료율을 비교하는 작은 비교 영역을 둔다.
- 12개 KPI를 의미별로 `금액`, `운영 상태`, `수익성` 그룹으로 나눠 우선순위를 만든다.
- 월별 주문에서 바로 확인할 수 있도록 `미완료/미배송 주문` 요약과 최대 5건의 바로가기 목록을 추가한다.
- 플랫폼·결제수단·구매계정별 표에 건수와 비중을 표시한다. 비중이 필요하면 현재 월 주문 수에서 계산하고, 별도 집계 테이블은 만들지 않는다.

#### 배치

- `max-w-screen-md`를 제거하고 데스크톱에서는 `요약 8열 + 확인할 주문 4열` 구조를 사용한다.
- 월 이동 헤더는 전체 폭의 상단에 두고, 대시보드로 돌아가기와 현재 월 선택을 함께 제공한다.
- KPI는 PC에서 4열, 태블릿에서 3열, 모바일에서 2열로 배치한다.
- 하단의 플랫폼·결제수단·구매계정 표는 PC에서 3열, 모바일에서 세로로 쌓는다.

### 4-4. `/menu-4` 주문 내역 자동 추천

대상 파일: `src/components/pages/crawl-orders-page.tsx`, `src/components/pages/chrome-extension-install-guide.tsx`

#### 추가할 정보

- 현재 `대기 추천`, `자동 매칭`, `확인 필요` 수치를 유지하고, 자동 매칭률 또는 확인 필요 비율을 추가한다.
- 추천 대기 목록의 PC 컬럼에 상품명/원문 제목, 구매일·구매금액, 플랫폼, 결제수단, 구매계정, 처리 액션을 명확히 분리한다.
- 계정별 크롤링 상태·마지막 실행 결과·익스텐션 설치 안내를 보조 패널에 둔다. 현재 저장된 실행 상태와 메시지만 사용하고, 실행 이력은 새로 만들지 않는다.
- 목록에 플랫폼·계정·확인 필요 기준 필터를 추가한다. 모바일에서는 상단의 접이식 필터로 제공한다.

#### 배치

- 데스크톱 기본안: `추천 대기 목록 8~9열 + 크롤링 상태/익스텐션 안내/처리 가이드 3~4열`.
- 크롬 익스텐션 설치 안내는 PC에서는 보조 패널로 이동해 목록의 세로 길이를 줄이고, 모바일에서는 현재처럼 목록 위에 전체 폭으로 둔다.
- 두 번째 입금 자동추천 화면은 `입금 계좌 요약 3~4열 + 입금 미완료 목록 8~9열`로 배치한다.
- 입금 요약에는 미처리 건수, 총 입금액, 추천 후보가 없는 건수, 높은 일치 후보가 있는 건수를 표시한다.
- 입금 행을 펼쳤을 때 후보 카드는 현재의 미완료/완료 구분, 유사도 배지, 입금금액 일치, 완료처리/매핑완료 액션을 유지하고, PC에서는 행 아래 넓은 후보 영역을 사용한다.
- 모바일에서는 현재의 2페이지 스와이프와 한 열 목록을 유지한다. 현재 보고 있는 추천 페이지만 마운트하는 최적화도 유지한다.

#### 선택이 필요한 부분

- 현재 좌우 화살표·스와이프 슬라이더를 PC에서도 유지할지, PC에서는 상단 탭/세그먼트로 바꿀지는 아래 질문에서 결정한다.
- 양쪽 추천 화면을 동시에 마운트하면 PC 폭은 더 잘 쓸 수 있지만, 입금 추천 데이터와 주문 추천 데이터가 동시에 조회될 수 있으므로 기본안은 한 화면만 마운트하는 것이다.

### 4-5. `/orders/new` 주문 추가

대상 파일: `src/components/pages/new-order-page.tsx`, `src/components/orders/order-detail-form.tsx`

#### 추가할 정보

- 우측 요약 패널에 입력 중인 상품명·구매일·구매금액·선택 플랫폼·결제수단·구매계정·배송/입금 상태를 실시간으로 표시한다.
- 필수값 충족 여부와 저장 가능 여부를 체크리스트로 보여준다.
- 구매계정을 여러 개 선택했을 때 생성될 주문 수와 주문번호가 비워지는 규칙을 저장 전에 안내한다.
- 입금금액이 입력되면 구매금액 대비 차액/예상 수익을 보여주고, 미입력 상태는 “아직 계산하지 않음”으로 구분한다.
- 기존 카톡 붙여넣기 정보·구매정보·AI 리뷰·완료정보 카드의 내용을 PC에서 두 열로 나눠 긴 스크롤을 줄인다.

#### 배치

- 데스크톱: `입력 폼 8열 + 실시간 요약/저장 전 확인 4열`.
- 폼 안에서는 구매정보를 먼저 두고, 카톡 템플릿·AI 리뷰·완료정보를 보조 순서로 둔다.
- 저장 액션은 PC에서 우측 요약 패널 상단 또는 하단에 sticky로 두고, 모바일에서는 폼 마지막의 전체 폭 액션으로 둔다.
- 현재 `OrderDetailForm`의 저장·완료·확인창·템플릿·AI 리뷰 동작을 재구현하지 않고 기존 상태를 확장해 요약에 연결한다.

### 4-6. `/orders/detail` 주문 상세

대상 파일: `src/components/pages/order-detail-page.tsx`, `src/components/orders/order-detail-form.tsx`

#### 추가할 정보

- 헤더에 상품명, 주문 상태, 구매일, 플랫폼/구매계정, 목록으로 돌아가기 및 저장/완료 액션을 배치한다.
- 우측 요약 패널에 구매금액·입금금액·수익·입금 상태·배송 상태·리뷰 사진/글자 수를 표시한다.
- `created_at`·`updated_at`은 “최초 등록/최근 수정”으로만 보여주고, 실제로 존재하지 않는 상세 변경 이력처럼 표현하지 않는다.
- 미배송·미입금·리뷰 미작성 등 현재 주문 데이터로 판단할 수 있는 확인 항목을 우선순위 순으로 보여준다.

#### 배치

- 데스크톱: `주문 편집 폼 8열 + 상태/금액/다음 행동 4열`.
- 폼의 기존 섹션은 모바일에서 현재 순서를 유지하고, PC에서는 독립적인 카드끼리 2열로 배치한다.
- 완료·저장 액션은 데스크톱에서 우측 패널에 고정할 수 있고, 모바일에서는 하단 메뉴와 충돌하지 않는 일반 액션 영역으로 둔다.

### 4-7. `/settings` 설정 홈과 하위 뷰

대상 파일: `src/components/pages/settings-page.tsx`, `src/components/settings/settings-panel.tsx`

#### 추가할 정보

- 설정 홈에 닉네임/이메일 카드와 함께 템플릿 수, 플랫폼 수, 결제수단 수, 구매계정 수를 요약한다.
- 각 메뉴에 현재 등록 건수 또는 “설정 필요” 배지를 추가한다.
- 계정 화면에는 닉네임·이메일·로그아웃을 명확히 묶고, 개인정보를 더 수집하는 항목은 추가하지 않는다.
- 템플릿 화면에는 제목뿐 아니라 저장된 필드가 몇 개인지, 최근 수정 시각을 표시한다. 실제 내용 미리보기는 민감 정보 노출 여부를 고려해 선택한다.
- AI 설정 화면은 왼쪽에 입력 목적·개인정보 주의사항, 오른쪽에 기존 폼을 배치한다.
- 플랫폼·결제수단·구매계정 화면은 목록, 색상/숨김/삭제 상태, 새 항목 추가를 한 영역에서 확인할 수 있도록 한다. 사용 중인 주문 수를 표시하려면 추가 집계 조회가 필요하므로 결정 후 진행한다.

#### 배치

- 데스크톱: 글로벌 사이드바의 `설정` 항목을 펼쳐 하위 설정 메뉴를 보여주고, 설정 화면 안에 별도의 두 번째 사이드바는 만들지 않는다. 선택한 설정 화면은 글로벌 사이드바 오른쪽의 전체 콘텐츠 영역을 사용한다.
- 설정 홈에서는 계정 요약을 전체 폭으로, 나머지 설정 링크를 2~3열 카드 또는 그룹으로 배치한다.
- 모바일에서는 현재처럼 한 화면에서 메뉴를 선택하고 하위 화면에 들어가는 드릴다운을 유지한다.
- 글로벌 사이드바의 하위 메뉴와 현재 `SettingsPanel`의 내부 view 상태를 연결하고, `?view=account` 진입 방식은 우선 유지한다. URL 라우트 분리는 별도 요구가 있을 때만 한다.

### 4-8. `/settings/purchase-templates/new` 및 `/settings/purchase-templates/detail`

대상 파일: `src/components/pages/new-purchase-template-page.tsx`, `src/components/pages/purchase-template-detail-page.tsx`, `src/components/purchase-templates/purchase-info-template-form.tsx`

#### 추가할 정보

- 입력 필드를 `식별 정보`, `배송 정보`, `계정/정산 정보`로 묶어 사용자가 빠르게 찾게 한다.
- 우측에 입력값으로 생성되는 카톡 붙여넣기 결과 미리보기를 제공한다. 미리보기는 기존 `buildKakaoPasteLine` 공통 로직을 재사용한다.
- 제목 필수 여부, 입력된 필드 수, 복사 가능한 상태를 보여준다.
- 수정 화면에서는 최근 수정 시각과 “설정 목록으로 돌아가기”를 명확히 두며, 삭제나 복제는 별도 요구가 없으면 추가하지 않는다.

#### 배치

- 데스크톱: `템플릿 입력 7열 + 실시간 미리보기/도움말 5열`.
- 저장 버튼은 헤더와 폼 마지막 중 한 곳에서만 제공해 중복 액션을 피한다. 긴 폼에서는 우측 미리보기 상단에 sticky 저장 상태를 둘 수 있다.
- 모바일에서는 입력 폼을 먼저, 미리보기를 그 아래에 배치하고, 저장 버튼은 전체 폭으로 둔다.

### 4-9. `/login` 및 비로그인 상태

대상 파일: `src/components/pages/login-page.tsx`, `src/components/pages/home-page.tsx`, `src/components/auth/landing-auth-panel.tsx`, `src/components/auth/login-form.tsx`

- `/login`과 로그인 전 `/`는 같은 서비스 소개형 랜딩 구조를 공유한다. 데스크톱 기본안은 `서비스가 해결하는 일/간단한 안내 5열 + 로그인 카드 7열`의 split layout이다.
- 안내 영역에는 구매장부·자동추천·대시보드의 핵심 용도와 로그인 후 첫 행동만 간단히 보여주고, 마케팅 문구를 과도하게 늘리지 않는다.
- 모바일은 현재 중앙 로그인 카드와 안내 문구를 유지한다.
- 로그인 후 `/`는 기존 구매장부로 전환한다. 비로그인 상태와 로그인 상태의 화면 목적이 혼동되지 않도록 로그인 전용 랜딩 콘텐츠와 구매장부 콘텐츠를 동시에 보여주지 않는다.
- 랜딩 안내는 `LandingAuthPanel`·`LoginForm`을 재사용해 `/login`과 홈의 비로그인 상태에서 문구·간격·모바일 동작이 달라지지 않게 한다.

### 4-10. 공통 크롬 익스텐션 안내

대상 파일: `src/components/pages/chrome-extension-install-guide.tsx`

- 자동추천 PC 보조 패널에 들어갈 수 있도록 현재 카드의 폭·버튼·접기 동작을 재사용한다.
- 모바일에서는 다운로드 버튼이 전체 폭으로 보이고 설치 단계는 접힌 상태를 유지한다.
- 익스텐션 버전·ZIP·설치 내용은 현재처럼 보존한다. 별도의 스토어 배포나 자동 업데이트 기능은 이번 계획에 포함하지 않는다.

## 5. 데이터·구현 범위 원칙

- 새 페이지를 만들기보다 기존 `HomePage`, `OrdersTable`, `OrdersDashboard`, `CrawlOrdersPage`, `OrderDetailForm`, `SettingsPanel`, `PurchaseInfoTemplateForm`을 확장한다.
- 공통 수치 계산은 `src/lib/dashboard-stats.ts`에 추가하고, 페이지별로 같은 계산을 복제하지 않는다.
- 템플릿 미리보기는 `src/lib/kakao-purchase-paste.ts`, 색상은 `src/lib/color.ts`, DB 조회는 기존 Supabase client/쿼리 구조를 재사용한다.
- Recharts를 도입하면 `package.json`과 `package-lock.json`에만 의존성을 추가하고, 차트 표시를 담당하는 클라이언트 컴포넌트와 수치 계산을 담당하는 `dashboard-stats.ts`를 분리한다. 차트 라이브러리가 주문 데이터 조회나 업무 상태 변경을 직접 담당하지 않게 한다.
- 완료 주문 전체 목록을 초기 로드하거나 자동추천 두 페이지의 대량 데이터를 동시에 조회해 화면을 넓히는 방식은 피한다.
- 새 요약에 필요한 값이 현재 화면에 이미 로드된 데이터로 계산되지 않으면 다음 순서로 판단한다.
  1. 현재 쿼리 결과로 계산할 수 있는지 확인한다.
  2. 가능하면 count/sum 같은 가벼운 집계 조회를 추가한다.
  3. 새 DB 컬럼·테이블이 꼭 필요한 경우에만 별도 승인을 받는다.
- DB 스키마를 변경하게 되면 변경 직후 전체 스키마를 재조회하고 `docs/guide_db.md`를 같은 작업에서 갱신한다.
- 새 컴포넌트/함수는 기존 책임으로 해결할 수 없는 경우에만 만들고, 변경 코드의 주석은 비개발자도 이해할 수 있는 한국어로 작성한다.

## 6. 구현 순서와 검증 기준

### 6-1. 결정 반영 및 공통 셸

- 아래 질문에 대한 답변을 반영해 데스크톱 내비게이션, breakpoint, 최대 폭, main/aside 비율을 확정한다.
- 공통 컨테이너·헤더·데스크톱 내비게이션·모바일 하단 메뉴의 중복 스타일을 정리한다.

검증: 1280px 이상에서 주요 페이지가 중앙의 좁은 카드 하나로 축소되지 않고, 390px 전후 모바일에서 가로 overflow와 하단 메뉴 충돌이 없다.

### 6-2. 업무 우선 화면

- `/` 구매장부와 `/menu-4` 자동추천에 요약·주의·빠른 액션을 먼저 배치한다.
- 기존 목록 동작, 후보 계산, 완료처리, 삭제, 익스텐션 다운로드를 보존한다.

검증: 사용자가 첫 화면에서 미완료/확인 필요 건수와 다음 처리 위치를 알 수 있고, 기존 처리 동작이 동일하다.

### 6-3. 분석 화면

- `/dashboard`와 `/dashboard/monthly`에 기간 비교, 월 이동, 확인할 주문, 분류별 요약을 배치한다.
- 월별 표와 상세 화면 사이 이동을 연결한다.

검증: 기간을 바꿨을 때 모든 KPI·비교·인사이트가 같은 범위를 사용하고, 주문이 없는 기간에도 빈 상태가 깨지지 않는다.

### 6-4. 입력·설정 화면

- 주문 추가/상세의 요약 패널과 설정/템플릿의 데스크톱 2단 레이아웃을 적용한다.
- 모바일에서는 입력 순서와 저장·완료 액션의 접근성을 다시 확인한다.

검증: 입력값과 우측 요약이 불일치하지 않고, 여러 구매계정 주문 생성·완료처리·템플릿 저장이 기존 규칙대로 동작한다.

### 6-5. 인증·공통 상태 및 최종 검증

- 로그인, 로딩, 오류, 빈 상태, 계정 메뉴를 넓은 셸과 모바일 셸에서 확인한다.
- `npm run lint`, `npm run build`를 실행한다.
- 최소한 다음 폭에서 주요 경로를 수동 점검한다: 390px, 768px, 1024px, 1280px, 1440px.
- 주요 경로: `/`, `/dashboard`, `/dashboard/monthly`, `/menu-4`, `/orders/new`, `/orders/detail`, `/settings`, 템플릿 추가/수정, `/login`.
- `git diff`로 DB·기존 업무 동작·크롬 익스텐션 파일에 불필요한 변경이 없는지 확인한다.

최종 완료 기준:

- PC에서 주요 업무 콘텐츠가 화면 폭에 맞게 확장되고, 확장된 공간에 사용자에게 의미 있는 요약·주의·빠른 액션이 있다.
- 모바일에서 주요 정보와 완료/저장/이동 액션이 가려지지 않는다.
- 새로운 정보가 기존 데이터와 모순되지 않고, 로딩·빈 데이터·오류 상태가 명확하다.
- 기존 DB 스키마를 변경하지 않았다면 `docs/guide_db.md` 변경은 없다. 스키마 변경을 선택했다면 해당 문서를 갱신한다.

## 7. 결정이 필요한 질문

아래는 구현 전에 답이 필요한 항목이다. 추천안은 기본값일 뿐이며, 답변에 따라 계획을 좁힌다.

### Q1. 데스크톱 내비게이션을 어떻게 할까요?

- **A (추천)**: `1024px` 이상에서 왼쪽 고정 사이드바, 모바일에서는 현재 하단 메뉴 유지.
- B: 사이드바를 추가하지 않고 현재 하단 메뉴를 모든 화면 폭에서 유지하며 콘텐츠만 넓힘.
- C: 사이드바 대신 상단 가로 내비게이션을 추가.

사이드바를 선택하면 PC 공간 활용과 페이지 이동성은 좋아지지만, 공통 셸 변경 범위가 커진다.


### Q2. PC 콘텐츠의 최대 폭을 어디까지 허용할까요?

- **A (추천)**: 1280~1440px의 유동 폭. 표·카드가 필요한 만큼만 넓어지게 함.
- B: 최대 1280px로 제한해 시선 이동을 줄임.
- C: 최대 1600px까지 사용해 대형 모니터의 표·보조 패널을 적극 활용.

답변: A

### Q3. PC에서 가장 중요한 사용자 업무는 무엇인가요?

- **A (추천)**: 미완료 주문·입금 매칭·자동추천 확인을 빨리 처리하는 운영 중심.
- B: 구매금액·입금금액·수익·회수 현황을 분석하는 재무 중심.
- C: AI 리뷰·카톡 정보·템플릿 작성 같은 콘텐츠 생성 중심.

이 답변이 구매장부와 각 보조 패널의 우선순위를 결정한다.

### Q4. 추가 정보는 어떤 데이터 범위까지 허용할까요?

- **A (추천)**: 현재 조회하는 DB 필드와 그로부터 계산되는 정보만 사용. 1차 DB 변경 없음.
- B: 화면 성능을 위해 기존 테이블의 count/sum 집계 쿼리까지 추가.
- C: 알림·작업 이력·사용량까지 저장할 새 테이블/컬럼을 추가.

C를 선택하면 DB 마이그레이션과 `docs/guide_db.md` 갱신이 별도 범위로 들어간다.

### Q5. 자동추천 PC 화면을 어떻게 보여줄까요?

- **A (추천)**: PC에서는 상단 탭/세그먼트로 주문 추천과 입금 추천을 전환하고, 현재 페이지 하나만 마운트.
- B: PC에서는 주문 추천과 입금 추천을 좌우 2열로 동시에 표시.
- C: 모바일과 PC 모두 현재 좌우 화살표·스와이프 슬라이더를 유지.

B는 PC 폭을 가장 많이 사용하지만 두 데이터 세트를 동시에 조회·렌더링할 수 있다.

### Q6. 구매장부의 우측 보조 영역은 무엇을 우선할까요?

- **A (추천)**: 오늘 확인할 일 + 빠른 작업 + 처리 우선순위.
- B: 최근 주문/최근 완료 내역과 활동 흐름.
- C: 플랫폼·구매계정별 금액 요약.

현재 별도 활동 로그는 없으므로 B는 주문의 `created_at`·상태만으로 구성하고, 실제 이벤트 이력은 표현하지 않는다.

### Q7. 주문 추가·상세 화면은 어떤 밀도로 만들까요?

- **A (추천)**: PC 2열 폼 + 우측 실시간 요약/상태 패널, 모바일 1열.
- B: 폼 자체만 1200px까지 넓히고 보조 패널은 추가하지 않음.
- C: 입력 영역과 AI/완료정보를 탭으로 분리.

A는 현재 폼을 재사용하면서 PC 공간과 입력 확인성을 동시에 높이는 안이다.

### Q8. 설정 화면의 PC 구조는 무엇이 좋을까요?

- **A (추천)**: 왼쪽 설정 메뉴를 계속 보이고 오른쪽에 선택 화면을 표시.
- B: 설정 홈을 2~3열 카드 허브로 만들고 선택하면 현재처럼 하위 화면으로 이동.
- C: 모바일과 동일한 드릴다운을 PC에서도 유지.

### Q9. 템플릿 입력 화면에 미리보기를 추가할까요?

- **A (추천)**: PC 우측에 카톡 붙여넣기 결과 실시간 미리보기, 모바일에서는 폼 아래.
- B: 입력 폼만 넓히고 미리보기는 추가하지 않음.
- C: 미리보기는 `복사하기`를 눌렀을 때만 모달로 표시.

### Q10. 분석 화면에 그래프를 어느 정도 사용할까요?

- **A (추천)**: 기존 표·수치와 CSS 막대/증감 배지만 사용하고 차트 라이브러리는 추가하지 않음.
- B: 기존 의존성으로 가능한 간단한 그래프만 추가.
- C: 차트 라이브러리를 새로 도입해 추이·분포 차트를 적극적으로 추가.

### Q11. 현재 동작하지 않는 알림 아이콘은 어떻게 할까요?

- **A (추천)**: 이번 작업에서 미완료/확인 필요 요약으로 연결되는 알림 패널을 구현.
- B: 실제 알림 데이터가 없으므로 아이콘을 제거.
- C: 알림 센터와 읽음 상태를 새 DB 구조로 설계.

C는 별도 제품 기능과 DB 범위가 필요하다.

### Q12. 로그인 화면도 PC 정보 밀도 개선 대상에 포함할까요?

- **A (추천)**: 포함하되 split layout은 로그인 전용으로만 적용.
- B: 인증 화면은 집중을 위해 현재 중앙 카드 구조를 유지.
- C: 로그인과 비로그인 홈 모두 제품 소개형 랜딩 화면으로 확장.

### Q13. 자동추천의 크롬 익스텐션 안내는 어디에 둘까요?

- **A (추천)**: PC에서는 자동추천 보조 패널, 모바일에서는 현재 목록 위 전체 폭.
- B: 현재처럼 두 화면 모두 목록 위 전체 폭.
- C: 설정에 별도 “연동/익스텐션” 메뉴를 추가하고 자동추천에서는 짧은 링크만 표시.

### Q14. 새로운 요약 수치의 집계 시점을 어떻게 할까요?

- **A (추천)**: 첫 화면은 빠르게 열고, 상세 금액 집계는 비동기로 채움. 기존 lazy loading 유지.
- B: 모든 요약 데이터가 준비될 때까지 화면을 기다림.
- C: 별도 서버 집계/API를 구축.

### Q15. 주문 이력/알림 데이터가 필요할까요?

- **A (추천)**: 이번에는 현재 행의 생성일·수정일·상태만 표시하고 새 이력은 만들지 않음.
- B: 주문 상태 변경·입금 매칭·삭제 등의 이벤트 이력을 추가.
- C: 이력과 알림을 함께 새 도메인 기능으로 설계.

### Q16. 이번 계획의 구현 범위를 어디까지로 확정할까요?

- **A (추천)**: 위에 적은 모든 라우트와 설정 하위 뷰를 대상으로 하되, 핵심은 `/`, `/dashboard`, `/dashboard/monthly`, `/menu-4`, 주문 입력/상세, 설정.
- B: 업무 핵심 5개 화면(`/`, `/dashboard`, `/dashboard/monthly`, `/menu-4`, 주문 상세)만 먼저 구현.
- C: 공통 셸과 PC 폭 조정만 먼저 하고 정보 추가는 다음 단계로 분리.

## 8. 답변 반영 기록

최종 선택을 아래에 기록했다. 선택하지 않은 제안은 구현 범위에서 제거한다.

- Q1: A
- Q2: A
- Q3: A
- Q4: B
- Q5: A
- Q6: A
- Q7: A
- Q8: A (설정 메뉴를 상시 표시) → 후속 구체화: 글로벌 사이드바의 설정 하위 메뉴로 통합
- Q9: A
- Q10: C (새 차트 라이브러리 도입) → 후속 확정: Recharts 사용
- Q11: B
- Q12: C (로그인과 비로그인 홈 모두 랜딩화) → 후속 확정: `/login`과 비로그인 `/`가 같은 소개형 로그인 흐름을 공유하고, 로그인 후 `/`는 구매장부로 전환
- Q13: A
- Q14: A
- Q15: A
- Q16: A

### 후속 질문 답변

- 차트 라이브러리: **A — Recharts**
- 비로그인 랜딩 흐름: **A — `/login`과 비로그인 `/`가 같은 소개형 랜딩 + 로그인 카드를 공유하고, 로그인 후 `/`는 구매장부로 사용**
- 글로벌 사이드바와 설정 메뉴: **B — 글로벌 사이드바의 설정 항목을 펼쳐 하위 메뉴를 보여주고 설정 내부의 별도 사이드바는 제거**
