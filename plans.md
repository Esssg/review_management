# PWA 구매 예정 알림 및 알림센터 구현 계획

작성일: 2026-08-15
상태: 구현 완료 — 서버·앱 1차 구현 및 가능한 검증 완료

## 목적

주문에 저장된 `scheduled_purchase_at`을 기준으로 구매 전 알림을 보내고, PWA 안에서 알림 내역·미확인 개수·주문 이동을 관리한다.

이번 1차 범위는 구매 예정 시각 알림이다. 새 크롤링 주문 알림이나 입금 추천 알림은 공통 알림 구조를 재사용할 수 있도록 확장 지점만 고려하고, 별도 발송 규칙은 추가하지 않는다.

## 현재 확인된 기반

- `orders.scheduled_purchase_at` 컬럼과 구매 예정 시각 입력·수정 UI가 이미 있다.
- 구매장부의 `HomeOperationsQueue`가 Today's Queue를 담당한다.
- 모바일 구매장부 상단의 현재 `Bell` 아이콘은 Today's Queue를 여는 용도로 사용 중이다.
- `/orders/detail?id=...`로 주문 상세 화면에 이동할 수 있다.
- PWA manifest와 설치 안내는 있지만 서비스 워커, Push 구독, 서버 푸시 발송, 앱 알림 내역 저장은 아직 없다.

## 확정된 요구사항

### 1. 구매 예정 알림

구매 예정 시각이 저장된 미완료 주문에 대해 다음 두 번 알림을 보낸다.

- 구매 예정 시각 10분 전
- 구매 예정 시각

알림 제목·내용은 주문의 `title`(카톡방 이름)을 사용해 다음 문구로 만든다.

- 10분 전 단건: `[카톡방이름] 구매 10분 전 입니다. 물품 정보를 보려면 알람을 확인하세요!`
- 10분 전 다건: `[카톡방이름 외 x건] 구매 10분 전 입니다. 물품 정보를 보려면 알람을 확인하세요!`
- 예정 시각 단건: `[카톡방이름] 구매 예정 시간 입니다! 물품 정보를 보려면 알람을 확인하세요!`
- 예정 시각 다건: `[카톡방이름 외 x건] 구매 10분 전 입니다. 물품 정보를 보려면 알람을 확인하세요!`

10분 전 알림을 사용자가 확인하지 않은 상태에서 정각 알림을 발송하면, 10분 전 알림은 실제 행을 삭제하지 않고 `read_at`을 기록해 읽음 처리한다. 화면의 미확인 목록과 개수에서는 즉시 제외한다.

정각 알림 발송이 실패하면 10분 전 알림을 자동으로 읽음 처리하지 않고 재시도할 수 있게 한다.

### 2. 단건 알림과 묶음 알림

- 알림 대상 주문이 한 건이면 알림을 눌렀을 때 해당 주문 상세로 이동한다.
- 구매 예정 시각이 동일한 주문 상세 건은 같은 묶음으로 처리한다.
- 같은 발송 단위에 여러 주문이 포함되면 묶음 알림으로 보낸다.
- 묶음 알림을 누르면 해당 묶음에 포함된 주문만 보여주는 별도 모달을 연다.
- 모달 안에서 주문을 누르면 해당 주문 상세로 이동한다.
- 모달 바깥 영역이나 `X` 버튼을 누르면 이동 없이 모달만 닫는다.
- 알림 아이콘에는 미확인 알림이 연결된 주문의 개수를 표시한다. 같은 주문의 10분 전·정각 알림은 하나의 주문으로 센다.
- 묶음 알림을 눌러 모달을 여는 순간 해당 묶음의 모든 주문을 읽음 처리한다.

### 3. 기존 Today's Queue와 새 알림 아이콘 분리

- 현재 Today's Queue의 운영 정보는 유지한다.
- Today's Queue를 여는 기존 `Bell` 아이콘은 `ListTodo` 또는 `ClipboardList` 계열 아이콘으로 바꿔 알림 아이콘과 역할을 분리한다.
- 새 `Bell` 아이콘은 푸시받은 알림 내역과 미확인 개수를 보여준다.
- 새 알림 아이콘은 로그인한 모든 주요 화면의 상단 액션 영역에 표시한다.
- Today's Queue 안의 입금 미완료·미배송·오늘 구매 일정·완료 정보 확인 항목은 각각 의미에 맞는 기존 아이콘을 유지한다.

### 4. 앱 푸시 권한 설정

설정 화면에 `앱 사용 권한 설정하기` 버튼을 추가한다.

버튼을 누르면 앱 푸시 권한 설정 팝업을 열고, 팝업 안의 실행 버튼을 사용자가 누르면 브라우저의 알림 권한 요청과 기기별 Push 구독 등록을 시작한다.

팝업은 다음 상태를 표시한다.

- 아직 설정하지 않음: 권한 설정 버튼 표시
- 허용됨: 현재 기기가 알림을 받을 수 있다는 안내와 해제 버튼
- 차단됨: 브라우저·운영체제 설정에서 다시 허용하는 방법 안내
- 현재 브라우저에서 지원하지 않음: 지원 조건 안내

## 제안하는 전체 동작

```text
주문에 구매 예정 시각 저장
        ↓
서버 예약 작업이 10분 전·정각 시점 감지
        ↓
알림 내역 저장 + Web Push 발송
        ↓
서비스 워커가 운영체제 알림 표시
        ↓
사용자가 알림 클릭
        ├─ 단건: 주문 상세 이동
        └─ 다건: 주문 목록 모달 표시
```

브라우저가 열려 있을 때의 타이머에 의존하지 않는다. 앱이 종료되어도 동작해야 하므로 Supabase Edge Function과 예약 실행 작업이 서버에서 시간을 확인한다.

## 데이터 설계 초안

현재 테이블에 구매 예정 시각이 있으므로 `orders` 컬럼은 변경하지 않는다. 다음 두 개의 Review Manager 전용 테이블을 추가하는 방향으로 설계한다.

### `push_subscriptions`

기기·브라우저별 Web Push 구독 정보를 저장한다.

- `id`
- `user_id`
- `endpoint`
- `p256dh`
- `auth`
- `device_label`
- `enabled`
- `created_at`
- `last_seen_at`

한 사용자가 휴대폰·PC·태블릿을 동시에 사용할 수 있으므로 사용자당 여러 행을 허용한다. 모든 행은 사용자 소유 RLS를 적용한다.

### `app_notifications`

푸시 발송 내역과 앱 알림센터에 표시할 정보를 저장한다.

- `id`
- `user_id`
- `order_id` — 주문별 알림 기록을 연결
- `group_id` — 구매 예정 시각이 같은 주문을 연결
- `notification_type` — `purchase_10m`, `purchase_due`
- `title`
- `body`
- `target_href` 또는 대상 식별 정보
- `scheduled_for`
- `sent_at`
- `read_at`
- `created_at`

알림 기록은 주문별로 저장하고, 같은 예정 시각의 주문에는 같은 `group_id`를 부여한다. 미확인 개수는 `read_at IS NULL`인 기록을 주문 ID 기준으로 중복 제거해 계산한다. 같은 주문·같은 예정 시각·같은 알림 종류가 중복 발송되지 않도록 유일성 또는 서버 idempotency 기준을 둔다. 일정이 변경되거나 주문이 완료·삭제되면 아직 발송하지 않은 예약 알림은 무효화한다.

읽음 처리는 실제 삭제 대신 `read_at`을 기록한다. 화면에서는 읽은 알림을 숨겨 사용자가 보기에는 삭제된 것처럼 동작하게 한다.

알림 아이콘을 눌렀을 때는 미확인 알림만 표시한다. 단건 알림을 누르면 해당 주문의 미확인 알림을 읽음 처리하고 주문 상세로 이동한다. 묶음 알림을 누르면 해당 그룹의 모든 주문 알림을 읽음 처리한 뒤 그룹 모달을 연다.

새 테이블은 현재 `AGENTS.md`의 DB 허용 목록에 없으므로 실제 구현 단계에서 다음을 함께 처리한다.

- 두 테이블이 Review Manager 소유 테이블임을 확인
- 사용자 소유 RLS와 인증 사용자 권한 적용
- `database-guide.md`의 허용 목록·컬럼·인덱스 갱신
- Supabase 타입 계약 갱신

## 구현 단계

### 0단계: 확정된 UX와 규칙을 공통 계약으로 고정

- 10분 전·예정 시각 알림 문구를 상수 또는 알림 생성 규칙으로 고정한다.
- 미확인 개수는 주문 기준으로 계산한다.
- 구매 예정 시각이 같은 주문만 같은 `group_id`로 묶는다.
- 묶음 알림을 누르면 전체 읽음 처리 후 주문 카드 모달을 연다.
- 알림센터에는 미확인 알림만 표시한다.
- 묶음 모달은 구매장부 주문 카드 정보를 재사용한다.

### 1단계: 알림 도메인 계약과 공통 타입 설계

- 단건·묶음 알림의 데이터 구조 정의
- 10분 전 알림과 정각 알림의 상태 전이 정의
- 미확인 개수 계산 규칙 정의
- 주문 상세·구매 장부·묶음 모달의 이동 정보 정의
- 일정 변경·주문 완료·주문 삭제 시 알림 무효화 규칙 정의

### 2단계: Supabase 스키마와 예약 발송 기반 추가

- `push_subscriptions`, `app_notifications` 마이그레이션 작성
- 사용자 소유 RLS·인덱스·중복 방지 제약조건 추가
- DB 가이드와 생성 타입 갱신
- `scheduled_purchase_at`을 기준으로 발송 대상 주문을 찾는 Edge Function 작성
- 서버 예약 작업이 10분 전·정각 알림을 한 번씩만 생성하도록 구현
- 정각 알림 발송 시 같은 주문의 미확인 10분 전 알림을 읽음 처리
- 실패한 발송은 재시도할 수 있도록 상태를 보존

### 3단계: PWA Web Push 연결

- `public/sw.js` 서비스 워커 추가
- 클라이언트에서 서비스 워커 등록
- VAPID 공개키·비밀키 설정
- 설정 팝업의 권한 요청 버튼 구현
- 기기별 Push 구독을 Supabase에 저장·갱신·비활성화
- 서비스 워커에서 `push` 이벤트를 받아 `showNotification()` 실행
- 알림 클릭 시 단건 주소 또는 묶음 모달 주소로 이동

푸시 본문에는 민감한 주문 정보 전체를 넣지 않고, 주문 ID·묶음 ID와 짧은 안내만 넣는다. 상세 정보는 앱이 열린 뒤 인증된 Supabase 조회로 가져온다.

### 4단계: 알림 아이콘과 알림센터 UI

- 공통 `NotificationBell`과 미확인 개수 뱃지 추가
- 로그인한 화면의 상단 액션 영역에 배치
- 알림 아이콘 클릭 시 현재 사용자 알림 내역 패널 또는 모달 표시
- 단건 알림과 묶음 알림을 구분해 표시
- 로딩·빈 상태·권한 미설정·오류 상태 추가
- 알림을 클릭하면 읽음 처리 후 대상 화면으로 이동
- 묶음 알림 클릭 시 해당 그룹의 주문만 조회하는 별도 모달 표시
- 모달의 주문 클릭, 바깥 영역 클릭, `X` 클릭 동작 구현

### 5단계: 기존 Today's Queue UI 정리

- 현재 `HomeOperationsQueue`의 내용과 링크는 유지
- 모바일에서 Queue를 여는 `Bell`을 업무 목록 의미의 아이콘으로 교체
- 새 알림 `Bell`과 Queue 아이콘의 위치·간격·접근성 이름 정리
- 모바일 상단, 데스크톱 상단 또는 사이드바에서 역할이 중복되지 않도록 확인

### 6단계: 테스트와 브라우저 검증

- 예정 시각·10분 전 시각 계산 테스트
- 한국 시간 표시와 UTC 저장 경계 테스트
- 같은 알림의 중복 발송 방지 테스트
- 정각 알림 발송 시 10분 전 미확인 알림 자동 읽음 처리 테스트
- 단건 알림의 주문 상세 이동 테스트
- 묶음 알림의 주문 목록 모달·주문별 이동·닫기 테스트
- 미확인 개수와 읽음 처리 테스트
- 권한 허용·거부·지원하지 않는 브라우저 상태 테스트
- `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build`
- Playwright로 데스크톱·모바일 알림 아이콘과 모달 UI 확인
- Android PWA와 iPhone 홈 화면 PWA에서 실제 백그라운드 푸시 확인

## 확정된 결정

사용자 답변을 반영해 다음 규칙으로 확정한다.

| 항목 | 확정 내용 |
| --- | --- |
| 알림 시점 | 구매 예정 시각 10분 전, 구매 예정 시각 |
| 알림 문구 | 위 확정된 단건·다건 문구 사용 |
| 미확인 개수 | 알림 이벤트가 아니라 미확인 주문 개수로 계산 |
| 알림 묶음 기준 | 구매 예정 시각이 동일한 주문을 같은 묶음으로 처리 |
| 묶음 알림 읽음 처리 | 묶음 모달을 여는 순간 해당 묶음 전체를 읽음 처리 |
| 알림센터 목록 | 미확인 알림만 표시하고 읽음 처리된 알림은 숨김 |
| 알림 아이콘 위치 | 로그인한 모든 주요 화면의 상단 액션 영역 |
| 묶음 모달 정보 | 구매장부의 주문 카드 정보를 재사용 |
| 모달 닫기 | 바깥 영역 또는 `X`는 이동 없이 모달만 닫음 |

다건 문구의 `[카톡방이름 외 x건]`에서 `x`는 대표 카톡방 이름 외에 포함된 추가 주문 건수로 해석한다. 구현 전 문구 확인 시 이 해석을 함께 검토한다.

## 구현 시작 조건

이 문서는 사용자 답변을 반영한 확정 계획이다. 다음 구현 단계에서는 먼저 1단계의 공통 타입·상태 계약을 작성하고, 그 계약을 기준으로 Supabase 스키마·예약 발송·PWA 서비스 워커·알림 UI를 순서대로 구현한다.

## 실제 구현 진행 현황

- Supabase에 `push_subscriptions`, `app_notifications` 테이블과 사용자 소유 RLS를 적용했습니다.
- `app_notifications`는 브라우저에 조회와 `read_at` 변경만 허용하고, 서버 발송 함수만 생성·발송 상태를 변경합니다.
- `process-purchase-notifications` Edge Function을 추가해 10분 전·정각 발송, 동일 예정 시각 묶음, 중복 방지, 일정 변경/완료/삭제 무효화, 410 만료 구독 비활성화, 정각 발송 후 10분 전 읽음 처리를 구현했습니다.
- `vm-app-01`에는 VAPID 비밀값, 예약 호출 비밀값, 함수 파일, 매분 사용자 cron을 설치하고 `supabase-edge-functions`만 재생성했습니다.
- `public/sw.js`, 공통 알림 Provider/Bell, 묶음 주문 모달, 설정의 `앱 사용 권한 설정하기` 카드를 추가했습니다.
- `lint`, TypeScript, 단위 테스트 11개, production build, Edge Function 번들·인증 smoke를 통과했습니다.
- 로컬 브라우저에서 서비스 워커 `/` scope 등록과 `/sw.js` 응답을 확인했습니다. 로컬 환경에 E2E 테스트 계정 환경변수가 없어 인증 후 알림 벨·설정 화면의 브라우저 검증은 남아 있습니다.

구현은 완료했지만, 실제 기기에서 권한 허용 후 Push가 도착하는 최종 수동 확인은 배포된 웹 앱에서 테스트 계정으로 한 번 진행해야 합니다. 이 확인은 코드·DB·서버 예약 작업이 이미 적용된 뒤의 운영 수신 확인 단계입니다.
